// Generated by the Freon Language Generator.
import type { KtList, Sentence } from 'net.akehurst.language-agl-processor';
import { FreTyperDefSyntaxAnalyserPart } from "./index.js";
import { MetaElementReference } from '../../languagedef/metalanguage/MetaElementReference.js';
import { FreMetaLangElement } from '../../languagedef/metalanguage/FreMetaLangElement.js';
import { FreParseLocation } from "../../utils/index.js";

export enum PrimValueType {
    string = "string",
    number = "number",
    boolean = "boolean",
    identifier = "identifier"
}

/**
 *   Class MetaTyperSyntaxAnalyser is the main syntax analyser.
 *   The actual work is being done by its parts, one for each model unit,
 *   and one common part that contains the methods used in multiple units.
 *
 */
export class FreTyperSyntaxAnalyser {
    locationMap: any;
    filename: string = '';
    private _unit_TyperDef_analyser: FreTyperDefSyntaxAnalyserPart = new FreTyperDefSyntaxAnalyserPart(this);

    clear(): void {
        throw new Error("Method not implemented.");
    }

    transform<T>(sppt: any): T {
        if (!!sppt.root) {
            return this.transformSharedPackedParseTreeNode(sppt.root) as unknown as T;
        } else {
            return null as T;
        }
    }

    public transformSharedPackedParseTreeNode(node: any): any {
        try {
            if (node.isLeaf) {
                return this.transformSharedPackedParseTreeLeaf(node);
            } else if (node.isBranch) {
                return this.transformSharedPackedParseTreeBranch(node);
            }
        } catch (e: unknown) {
            if (e instanceof Error) {
                if (e.message.startsWith("Syntax error in ") || e.message.startsWith("Error in MetaTyperSyntaxAnalyser")) {
                    throw e;
                } else {
                    // add more info to the error message
                    throw new Error(`Syntax error in "${node?.matchedText.trimEnd()}": ${e.message}`);
                }
            }
        }
    }

    private transformSharedPackedParseTreeLeaf(node: any): any {
        let tmp = (node?.nonSkipMatchedText).trim();
        if (tmp.length > 0) {
            if (tmp.startsWith('"')) {
                // stringLiteral, strip the surrounding quotes
                tmp = tmp.slice(1, tmp.length - 1);
                return tmp;
            } else if (tmp === "false") {
                // booleanLiteral
                return false;
            } else if (tmp === "true") {
                // booleanLiteral
                return true;
            } else if (Number.isInteger(parseInt(tmp, 10))) {
                // numberLiteral
                return parseInt(tmp, 10);
            } else {
                // identifier
                return tmp;
            }
        }
        return null;
    }

    private transformSharedPackedParseTreeBranch(branch: any): any {
        const brName: string = branch.name;
        const emptyKtList: KtList<object> = {
            asJsReadonlyArrayView: () => []
        };
        const emptySentence: Sentence = {
            locationFor: (start: number, length: number) => ({ line: 0, column: 0 })
        };
        if ("TyperDef" === brName) {
            return this._unit_TyperDef_analyser.transformTyperDef(branch, emptyKtList, emptySentence);
        } else if ("FretTypeConcept" === brName) {
            return this._unit_TyperDef_analyser.transformFretTypeConcept(branch, emptyKtList, emptySentence);
        } else if ("FretAnyTypeSpec" === brName) {
            return this._unit_TyperDef_analyser.transformFretAnyTypeSpec(branch, emptyKtList, emptySentence);
        } else if ("FretInferenceRule" === brName) {
            return this._unit_TyperDef_analyser.transformFretInferenceRule(branch, emptyKtList, emptySentence);
        } else if ("FretPropertyCallExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretPropertyCallExp(branch, emptyKtList, emptySentence);
        } else if ("FretSelfExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretSelfExp(branch, emptyKtList, emptySentence);
        } else if ("FretAnytypeExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretAnytypeExp(branch, emptyKtList, emptySentence);
        } else if ("FretVarCallExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretVarCallExp(branch, emptyKtList, emptySentence);
        } else if ("FretCreateExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretCreateExp(branch, emptyKtList, emptySentence);
        } else if ("FretPropInstance" === brName) {
            return this._unit_TyperDef_analyser.transformFretPropInstance(branch, emptyKtList, emptySentence);
        } else if ("FretFunctionCallExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretFunctionCallExp(branch, emptyKtList, emptySentence);
        } else if ("FretLimitedInstanceExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretLimitedInstanceExp(branch, emptyKtList, emptySentence);
        } else if ("FretWhereExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretWhereExp(branch, emptyKtList, emptySentence);
        } else if ("FretVarDecl" === brName) {
            return this._unit_TyperDef_analyser.transformFretVarDecl(branch, emptyKtList, emptySentence);
        } else if ("FretConformanceRule" === brName) {
            return this._unit_TyperDef_analyser.transformFretConformanceRule(branch, emptyKtList, emptySentence);
        } else if ("FretEqualsRule" === brName) {
            return this._unit_TyperDef_analyser.transformFretEqualsRule(branch, emptyKtList, emptySentence);
        } else if ("FretLimitedRule" === brName) {
            return this._unit_TyperDef_analyser.transformFretLimitedRule(branch, emptyKtList, emptySentence);
        } else if ("FretClassifierSpec" === brName) {
            return this._unit_TyperDef_analyser.transformFretClassifierSpec(branch, emptyKtList, emptySentence);
        } else if ("FretTypeRule" === brName) {
            return this._unit_TyperDef_analyser.transformFretTypeRule(branch, emptyKtList, emptySentence);
        } else if ("FretExp" === brName) {
            return this._unit_TyperDef_analyser.transformFretExp(branch, emptyKtList, emptySentence);
        } else if ("__fre_binary_FretExp" === brName) {
            return this._unit_TyperDef_analyser.transform__fre_binary_FretExp(branch, emptyKtList, emptySentence);
        } else if ("FretProperty" === brName) {
            return this._unit_TyperDef_analyser.transformFretProperty(branch, emptyKtList, emptySentence);
        } else if ("__fre_reference" === brName) {
            return this.transform__fre_reference(branch);
        } else {
            throw new Error(`Error in FreTyperSyntaxAnalyser: ${brName} not handled for node '${branch?.matchedText}'`);
        }
    }

    /**
     * Generic method to get the children of a branch. Throws an error if no children can be found.
     */
    public getChildren(branch: any): any {
        if (!!branch && !!branch.nonSkipChildren) {
            try {
                return branch.nonSkipChildren.toArray();
            } catch (e: unknown) {
                if (e instanceof Error) {
                    throw new Error(`Cannot follow branch: ${e.message} (${branch.matchedText.trimEnd()})`);
                }
            }
        }
        return null;
    }

    /**
     * Generic method to get the optional group of a branch. Throws an error if no group can be found.
     */
    public getGroup(branch: any) {
        // take the first element in the [0..1] optional group or multi branch
        let group: any = branch;
        let stop: boolean = false;
        while (!stop) {
            let nextOne: any = null;
            try {
                nextOne = group.nonSkipChildren.toArray()[0];
            } catch (e: unknown) {
                if (e instanceof Error) {
                    throw new Error(`Cannot follow group: ${e.message} (${group.matchedText})`);
                }
            }
            if (!nextOne.name.includes("multi") && !nextOne.name.includes("group")) {
                stop = true; // found a branch with actual content, return its parent!
            } else {
                group = nextOne;
            }
        }
        return group;
    }

    public transform__fre_reference(branch: any) {
        if (branch.name.includes("multi") || branch.name.includes("List")) {
            return this.transformSharedPackedParseTreeList<string>(branch, "::::");
        } else {
            return this.transformSharedPackedParseTreeLeaf(branch);
        }
    }

    /**
     * Generic method to transform references
     * ...FreNodeRef = identifier;
     */
    public freNodeRef<T extends FreMetaLangElement>(branch: any, typeName: string): MetaElementReference<T> {
        const referred: string | T = this.transformSharedPackedParseTreeNode(branch);
        if (referred === null || referred === undefined) {
            throw new Error(`Syntax error in "${branch?.parent?.matchedText}": cannot create empty reference`);
        } else if (typeof referred === "string" && (referred as string).length === 0) {
            throw new Error(`Syntax error in "${branch?.parent?.matchedText}": cannot create empty reference`);
        } else {
            return this.makeFreElementReferenceWithLocation(referred, typeName, branch);
        }
    }

    private makeFreElementReferenceWithLocation<T extends FreMetaLangElement>(referred: string | T, typeName: string, branch: any) {
        const result = typeof referred === 'string' 
            ? new MetaElementReference<T>(referred, typeName)
            : MetaElementReference.create<T>(referred, typeName);
        const location = FreParseLocation.create({ filename: this.filename, line: branch.location.line, column: branch.location.column });
        result.aglParseLocation = location;
        return result;
    }

    /**
     * Generic method to transform lists
     */
    public transformSharedPackedParseTreeList<T>(branch: any, separator?: string): T[] {
        const result: T[] = [];
        const children = this.getChildren(branch);
        if (!!children) {
            for (const child of children) {
                const element: any = this.transformSharedPackedParseTreeNode(child);
                if (element !== null && element !== undefined) {
                    if (separator === null || separator === undefined) {
                        result.push(element);
                    } else {
                        if (element !== separator) {
                            result.push(element);
                        }
                    }
                }
            }
        }
        return result;
    }

    /**
     * Generic method to transform lists of references
     */
    public transformSharedPackedParseTreeRefList<T extends FreMetaLangElement>(
        branch: any,
        typeName: string,
        separator?: string
    ): MetaElementReference<T>[] {
        const result: MetaElementReference<T>[] = [];
        const children = this.getChildren(branch);
        if (!!children) {
            for (const child of children) {
                const refName: any = this.transformSharedPackedParseTreeNode(child);
                if (refName !== null && refName !== undefined) {
                    if (separator === null || separator === undefined) {
                        result.push(this.makeFreElementReferenceWithLocation(refName, typeName, branch));
                    } else {
                        if (refName !== separator) {
                            result.push(this.makeFreElementReferenceWithLocation(refName, typeName, branch));
                        }
                    }
                }
            }
        }
        return result;
    }

    public location(branch: any) {
        return FreParseLocation.create({
            filename: this.filename,
            line: branch.location.line,
            column: branch.location.column
        });
    }
}