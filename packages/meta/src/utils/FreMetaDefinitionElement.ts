import { FreMetaBaseElement } from './FreMetaBaseElement.js';

// root of the inheritance structure of all elements that can be present in any of the definitions
export class FreMetaDefinitionElement extends FreMetaBaseElement {
    // Add any definition-specific properties here
}
