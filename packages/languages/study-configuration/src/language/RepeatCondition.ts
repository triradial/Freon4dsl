// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreUtils,
} from "@freon4dsl/core";
import { type RepeatExpression, type RepeatUnit, ScheduleExpression } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class RepeatCondition is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class RepeatCondition extends MobxModelElementImpl implements FreNode, RepeatExpression {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<RepeatCondition>): RepeatCondition {
        const result = new RepeatCondition(data.$id);
        if (notNullOrUndefined(data.maxRepeats)) {
            result.maxRepeats = data.maxRepeats;
        }
        if (notNullOrUndefined(data.repeatUnit)) {
            result.repeatUnit = data.repeatUnit;
        }
        if (notNullOrUndefined(data.until)) {
            result.until = data.until;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "RepeatCondition"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    maxRepeats!: number; // implementation of maxRepeats
    repeatUnit!: RepeatUnit; // implementation of part 'repeatUnit'
    until: ScheduleExpression | undefined; // implementation of part 'until'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "maxRepeats");
        this.maxRepeats = 0;

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "repeatUnit");

        observablepart(this, "until");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): RepeatCondition {
        const result = new RepeatCondition();
        if (notNullOrUndefined(this.maxRepeats)) {
            result.maxRepeats = this.maxRepeats;
        }
        if (notNullOrUndefined(this.repeatUnit)) {
            result.repeatUnit = this.repeatUnit.copy();
        }
        if (notNullOrUndefined(this.until)) {
            result.until = this.until.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<RepeatCondition>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.maxRepeats !== null && toBeMatched.maxRepeats !== undefined) {
            result = result && this.maxRepeats === toBeMatched.maxRepeats;
        }
        if (result && notNullOrUndefined(toBeMatched.repeatUnit)) {
            result = result && this.repeatUnit.match(toBeMatched.repeatUnit);
        }
        if (result && notNullOrUndefined(toBeMatched.until)) {
            if (this.until) {
                result = result && this.until.match(toBeMatched.until);
            }
        }
        return result;
    }
}
