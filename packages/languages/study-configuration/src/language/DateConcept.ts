// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
    FreUtils,
} from "@freon4dsl/core";
import { Month } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class DateConcept is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class DateConcept extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<DateConcept>): DateConcept {
        const result = new DateConcept(data.$id);
        if (notNullOrUndefined(data.dateAsString)) {
            result.dateAsString = data.dateAsString;
        }
        if (notNullOrUndefined(data.day)) {
            result.day = data.day;
        }
        if (notNullOrUndefined(data.year)) {
            result.year = data.year;
        }
        if (notNullOrUndefined(data.month)) {
            result.month = data.month;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "DateConcept"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    dateAsString!: string; // implementation of dateAsString
    day!: string; // implementation of day
    year!: string; // implementation of year

    month!: FreNodeReference<Month>; // implementation of reference 'month'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "dateAsString");
        this.dateAsString = "";
        observableprim(this, "day");
        this.day = "";
        observableprim(this, "year");
        this.year = "2025";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "month");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): DateConcept {
        const result = new DateConcept();
        if (notNullOrUndefined(this.dateAsString)) {
            result.dateAsString = this.dateAsString;
        }
        if (notNullOrUndefined(this.day)) {
            result.day = this.day;
        }
        if (notNullOrUndefined(this.year)) {
            result.year = this.year;
        }
        if (notNullOrUndefined(this.month)) {
            result.month = this.month.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<DateConcept>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.dateAsString !== null && toBeMatched.dateAsString !== undefined && toBeMatched.dateAsString.length > 0) {
            result = result && this.dateAsString === toBeMatched.dateAsString;
        }
        if (result && toBeMatched.day !== null && toBeMatched.day !== undefined && toBeMatched.day.length > 0) {
            result = result && this.day === toBeMatched.day;
        }
        if (result && toBeMatched.year !== null && toBeMatched.year !== undefined && toBeMatched.year.length > 0) {
            result = result && this.year === toBeMatched.year;
        }
        if (result && notNullOrUndefined(toBeMatched.month)) {
            result = result && this.month.match(toBeMatched.month);
        }
        return result;
    }

    /**
     * Convenience method for reference 'month'.
     * Instead of returning a 'FreNodeReference<Month>' object,
     * it returns the referred 'Month' object, if it can be found.
     */
    get $month(): Month | undefined {
        if (!!this.month) {
            return this.month.referred;
        }
        return undefined;
    }
}
