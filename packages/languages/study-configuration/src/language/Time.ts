// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    observableprim,
    observablepart,
    type FreExpressionNode,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
} from "@freon4dsl/core";
import { ScheduleExpression, TimeUnit } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate

/**
 * Class Time is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Time extends ScheduleExpression implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Time>): Time {
        const result = new Time(data.$id);
        if (notNullOrUndefined(data.value)) {
            result.value = data.value;
        }
        if (notNullOrUndefined(data.unit)) {
            result.unit = data.unit;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Time"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    value!: string; // implementation of value

    unit!: FreNodeReference<TimeUnit>; // implementation of reference 'unit'

    constructor(id?: string) {
        super(id);
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "value");
        this.value = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "unit");
        this.unit = FreNodeReference.create<TimeUnit>(TimeUnit.days, "TimeUnit");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Time {
        const result = new Time();
        if (notNullOrUndefined(this.value)) {
            result.value = this.value;
        }
        if (notNullOrUndefined(this.unit)) {
            result.unit = this.unit.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Time>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && toBeMatched.value !== null && toBeMatched.value !== undefined && toBeMatched.value.length > 0) {
            result = result && this.value === toBeMatched.value;
        }
        if (result && notNullOrUndefined(toBeMatched.unit)) {
            result = result && this.unit.match(toBeMatched.unit);
        }
        return result;
    }

    /**
     * Convenience method for reference 'unit'.
     * Instead of returning a 'FreNodeReference<TimeUnit>' object,
     * it returns the referred 'TimeUnit' object, if it can be found.
     */
    get $unit(): TimeUnit | undefined {
        if (!!this.unit) {
            return this.unit.referred;
        }
        return undefined;
    }
}
