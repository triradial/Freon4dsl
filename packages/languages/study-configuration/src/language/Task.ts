// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    observableprim,
    observablepart,
    observablepartlist,
    type FreNamedNode,
    FreParseLocation,
    notNullOrUndefined,
    matchElementList,
} from "@freon4dsl/core";
import { EventTask, Description, Step } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate

/**
 * Class Task is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Task extends EventTask implements FreNamedNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Task>): Task {
        const result = new Task(data.$id);
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.numberedSteps)) {
            result.numberedSteps = data.numberedSteps;
        }
        if (notNullOrUndefined(data.showDetails)) {
            result.showDetails = data.showDetails;
        }
        if (notNullOrUndefined(data.description)) {
            result.description = data.description;
        }
        if (notNullOrUndefined(data.steps)) {
            data.steps.forEach((x) => result.steps.push(x));
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Task"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    name!: string; // implementation of name
    numberedSteps!: boolean; // implementation of numberedSteps
    showDetails!: boolean; // implementation of showDetails
    description: Description | undefined; // implementation of part 'description'
    steps!: Step[]; // implementation of part 'steps'

    constructor(id?: string) {
        super(id);
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";
        observableprim(this, "numberedSteps");
        this.numberedSteps = false;
        observableprim(this, "showDetails");
        this.showDetails = false;

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "description");

        observablepartlist(this, "steps");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Task {
        const result = new Task();
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.numberedSteps)) {
            result.numberedSteps = this.numberedSteps;
        }
        if (notNullOrUndefined(this.showDetails)) {
            result.showDetails = this.showDetails;
        }
        if (notNullOrUndefined(this.description)) {
            result.description = this.description.copy();
        }
        if (notNullOrUndefined(this.steps)) {
            this.steps.forEach((x) => result.steps.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Task>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && toBeMatched.numberedSteps !== null && toBeMatched.numberedSteps !== undefined) {
            result = result && this.numberedSteps === toBeMatched.numberedSteps;
        }
        if (result && toBeMatched.showDetails !== null && toBeMatched.showDetails !== undefined) {
            result = result && this.showDetails === toBeMatched.showDetails;
        }
        if (result && notNullOrUndefined(toBeMatched.description)) {
            if (this.description) {
                result = result && this.description.match(toBeMatched.description);
            }
        }
        if (result && notNullOrUndefined(toBeMatched.steps)) {
            result = result && matchElementList(this.steps, toBeMatched.steps);
        }
        return result;
    }
}
