// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    observablepartlist,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { DateConcept, PatientVisit, DateRange } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class PatientHistory is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class PatientHistory extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<PatientHistory>): PatientHistory {
        const result = new PatientHistory(data.$id);
        if (notNullOrUndefined(data.patient_id)) {
            result.patient_id = data.patient_id;
        }
        if (notNullOrUndefined(data.id)) {
            result.id = data.id;
        }
        if (notNullOrUndefined(data.startOfStudyDate)) {
            result.startOfStudyDate = data.startOfStudyDate;
        } else {
            result.startOfStudyDate = DateConcept.create({});
        }
        if (notNullOrUndefined(data.patientVisits)) {
            data.patientVisits.forEach((x) => result.patientVisits.push(x));
        }
        if (notNullOrUndefined(data.patientNotAvailableDates)) {
            data.patientNotAvailableDates.forEach((x) => result.patientNotAvailableDates.push(x));
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "PatientHistory"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    patient_id!: string; // implementation of patient_id
    id!: string; // implementation of id
    startOfStudyDate!: DateConcept; // implementation of part 'startOfStudyDate'
    patientVisits!: PatientVisit[]; // implementation of part 'patientVisits'
    patientNotAvailableDates!: DateRange[]; // implementation of part 'patientNotAvailableDates'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "patient_id");
        this.patient_id = "";
        observableprim(this, "id");
        this.id = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "startOfStudyDate");

        observablepartlist(this, "patientVisits");
        observablepartlist(this, "patientNotAvailableDates");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): PatientHistory {
        const result = new PatientHistory();
        if (notNullOrUndefined(this.patient_id)) {
            result.patient_id = this.patient_id;
        }
        if (notNullOrUndefined(this.id)) {
            result.id = this.id;
        }
        if (notNullOrUndefined(this.startOfStudyDate)) {
            result.startOfStudyDate = this.startOfStudyDate.copy();
        }
        if (notNullOrUndefined(this.patientVisits)) {
            this.patientVisits.forEach((x) => result.patientVisits.push(x.copy()));
        }
        if (notNullOrUndefined(this.patientNotAvailableDates)) {
            this.patientNotAvailableDates.forEach((x) => result.patientNotAvailableDates.push(x.copy()));
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<PatientHistory>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.patient_id !== null && toBeMatched.patient_id !== undefined && toBeMatched.patient_id.length > 0) {
            result = result && this.patient_id === toBeMatched.patient_id;
        }
        if (result && toBeMatched.id !== null && toBeMatched.id !== undefined && toBeMatched.id.length > 0) {
            result = result && this.id === toBeMatched.id;
        }
        if (result && notNullOrUndefined(toBeMatched.startOfStudyDate)) {
            result = result && this.startOfStudyDate.match(toBeMatched.startOfStudyDate);
        }
        if (result && notNullOrUndefined(toBeMatched.patientVisits)) {
            result = result && matchElementList(this.patientVisits, toBeMatched.patientVisits);
        }
        if (result && notNullOrUndefined(toBeMatched.patientNotAvailableDates)) {
            result = result && matchElementList(this.patientNotAvailableDates, toBeMatched.patientNotAvailableDates);
        }
        return result;
    }
}
