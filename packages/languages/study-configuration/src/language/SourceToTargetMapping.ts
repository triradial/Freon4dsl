// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import { MobxModelElementImpl, observableprim, type FreNode, FreParseLocation, notNullOrUndefined, FreUtils } from "@freon4dsl/core";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class SourceToTargetMapping is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class SourceToTargetMapping extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<SourceToTargetMapping>): SourceToTargetMapping {
        const result = new SourceToTargetMapping(data.$id);
        if (notNullOrUndefined(data.sourceField)) {
            result.sourceField = data.sourceField;
        }
        if (notNullOrUndefined(data.targetField)) {
            result.targetField = data.targetField;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "SourceToTargetMapping"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    sourceField!: string; // implementation of sourceField
    targetField!: string; // implementation of targetField

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "sourceField");
        this.sourceField = "";
        observableprim(this, "targetField");
        this.targetField = "";

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): SourceToTargetMapping {
        const result = new SourceToTargetMapping();
        if (notNullOrUndefined(this.sourceField)) {
            result.sourceField = this.sourceField;
        }
        if (notNullOrUndefined(this.targetField)) {
            result.targetField = this.targetField;
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<SourceToTargetMapping>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.sourceField !== null && toBeMatched.sourceField !== undefined && toBeMatched.sourceField.length > 0) {
            result = result && this.sourceField === toBeMatched.sourceField;
        }
        if (result && toBeMatched.targetField !== null && toBeMatched.targetField !== undefined && toBeMatched.targetField.length > 0) {
            result = result && this.targetField === toBeMatched.targetField;
        }
        return result;
    }
}
