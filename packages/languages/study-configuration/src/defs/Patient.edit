editor default

Availability {
  [
    STAFFING
      Number of CRCs Normally Available: ${baselineStaff}

      Staffing Level Changes:
      ${staffLevels}

  ]
}

PatientNotAvailable {
  [
  ${dates}
  ]
}

StaffLevel {
  [ 
    # Staff: ${staffAvailable} on: ${dateOrRange replace=DateRangePickerComponent}
  ] trigger = "Staff Level"
}

DateRange {
  [${startDate} thru:${endDate} [button text="❌" boxRole="deleteDateRange"]]
}

DateConcept {
  [${dateAsString replace=DatePickerComponent startDate="30/09/2024"}]
}


PatientInfo {
  [
   ${patientHistories}
  ]
}

PatientHistory {
  [
   ID ${patient_id} 
   Patient completed: 
   ${patientVisits} 
   Not Available ${patientNotAvailableDates}
  ]
}

PatientVisit {
  [
      Visit: '${visit}' on  ${actualVisitDate}  [button text="❌" boxRole="deletePatientVisit"]
  ]
}

PatientHistoryUnit {
  [
    ${patientHistory}
  ]
}