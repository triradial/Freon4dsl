// Generated by the Freon Language Generator.
import { type FreNode, type FreNamedNode, FreNamespaceInfo, ownerOfType } from "@freon4dsl/core";
import { Event } from "../freon/language/index.js";
import { StudyConfigurationModelScoper } from "../freon/scoper/StudyConfigurationModelScoper.js";

/**
 * Class 'CustomStudyConfigurationModelScoper' is meant to be a convenient place to add any
 * custom code for scoping.
 */
export class CustomStudyConfigurationModelScoper extends StudyConfigurationModelScoper {
    /**
     *   Returns all elements that are visible in the namespace containing '_node'. Note that '_node' can
     *   be any node in the AST, not only namespaces!
     *
     *   When parameter '_metaType' is present, it returns all elements that are an instance of '_metaType'.
     *   There is no default setting for this parameter.
     *
     * @param _node
     * @param _metaType
     */
    getVisibleNodes(_node: FreNode, _metaType?: string): FreNamedNode[] {
        const defaultScope: FreNamedNode[] = super.getVisibleNodes(_node, _metaType);
        const parentEvent = ownerOfType(_node, "Event");
        const parent = defaultScope.findIndex(node => (node as Event) === parentEvent);
        if (parent !== -1) {
            defaultScope.splice(parent, 1);
        }
        return defaultScope;
    }

    /**
     * Returns all nodes and/or node references that represent namespaces which should be added to the namespace
     * represented by '_node'.
     *
     * @param _node
     */
    importedNamespaces(_node: FreNode): FreNamespaceInfo[] {
        return [];
    }

    /**
     * Returns all nodes and/or node references that represent namespaces which should be used to replace
     * the parent namespace of the namespace represented by '_node'.
     *
     * @param _node
     */
    alternativeNamespaces(_node: FreNode): FreNamespaceInfo[] {
        return [];
    }
}
