Periods:
  Period: `Screening`
  Description: " "
  Events:
    Event: `ICF (1A)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled on the start day of the study `-` 57 `days`
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:
      Task: `Task 1`
      Description: " "
      Steps:

    Event: `Screening (1B/1C)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled on the start day of the study `-` 56 `days`
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:
      Task: `Task 1`
      Description: " "
      Steps:

    Event: `BAE (2A)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled on the start day of the study `-` 21 `days`
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `BAE (2B)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled on the start day of the study `-` 7 `days`
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:

  Period: `Period`
  Description: " "
  Events:
    Event: `Randomization (3A)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled "" as the start day of the study
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3A)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Randomization (3A)` `started`
      with a window of 0 day(s) before and 0 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3B)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3A)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3C)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3B)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3D)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3C)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3E)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3D)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `PAC1 (4A)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3E)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose admin/PAC1 (3F/4B)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3E)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3G)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose admin/PAC1 (3F/4B)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3H)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3G)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Dose Admin (3I)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3H)` `started` `+` 4 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `PAC2 (4C)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `Dose Admin (3I)` `started` `+` 5 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `PAC2 (4D)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `PAC2 (4C)` `started` `+` 2 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `PAC2 (4E)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `PAC2 (4D)` `completed` `+` 11 `weeks`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

    Event: `Follow up (5)`
    This is a `site visit` that is also referred to as "<No Alternative Name>"
    Description: " "
    Schedule:
      First scheduled when `PAC2 (4E)` `started`
      with a window of 2 day(s) before and 2 day(s) after

      and then repeats
      limited to
    Tasks:

Tasks:

System Accesses:

Staffing:

Roles: