// Generated by the Freon Language Generator.
import { type KtList, type Sentence, type SpptDataNodeInfo } from "net.akehurst.language-agl-processor";
import { FreNodeReference, notNullOrUndefined } from "@freon4dsl/core";
import { Availability, StaffLevel, DateRange } from "../language/index.js";
import { PrimValueType, StudyConfigurationModelSyntaxAnalyser } from "./StudyConfigurationModelSyntaxAnalyser.js";

export class AvailabilitySyntaxAnalyserPart {
    mainAnalyser: StudyConfigurationModelSyntaxAnalyser;

    constructor(mainAnalyser: StudyConfigurationModelSyntaxAnalyser) {
        this.mainAnalyser = mainAnalyser;
    }

    /**
     * Method to transform branches that match the following rule:
     * Availability = 'STAFFING'
     *	 'Number' 'of' 'CRCs' 'Normally' 'Available:' stringLiteral
     *	 'Staffing' 'Level' 'Changes:'
     *	 StaffLevel* ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformAvailability(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): Availability {
        // console.log('4 transformAvailability called: ' + children.toString());
        let __baselineStaff: string;
        let __staffLevels: StaffLevel[];
        __baselineStaff = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[6] as unknown as string,
            PrimValueType.string,
        ); // RHSPrimEntry
        __staffLevels = (children.asJsReadonlyArrayView()[10] as KtList<any>).asJsReadonlyArrayView() as StaffLevel[]; // RHSPartListEntry

        return Availability.create({
            baselineStaff: __baselineStaff!,
            staffLevels: __staffLevels!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * StaffLevel = stringLiteral 'on:' DateRange ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformStaffLevel(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): StaffLevel {
        // console.log('4 transformStaffLevel called: ' + children.toString());
        let __staffAvailable: string;
        let __dateOrRange: DateRange;
        __staffAvailable = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[0] as unknown as string,
            PrimValueType.string,
        ); // RHSPrimEntry
        __dateOrRange = children.asJsReadonlyArrayView()[2] as DateRange; // RHSPartEntry

        return StaffLevel.create({
            staffAvailable: __staffAvailable!,
            dateOrRange: __dateOrRange!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }
}
