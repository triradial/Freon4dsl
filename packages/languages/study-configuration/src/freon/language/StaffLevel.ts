// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreUtils,
} from "@freon4dsl/core";
import { DateConcept, DateRange } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class StaffLevel is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class StaffLevel extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<StaffLevel>): StaffLevel {
        const result = new StaffLevel(data.$id);
        if (notNullOrUndefined(data.staffAvailable)) {
            result.staffAvailable = data.staffAvailable;
        }
        if (notNullOrUndefined(data.startDate)) {
            result.startDate = data.startDate;
        } else {
            result.startDate = DateConcept.create({});
        }
        if (notNullOrUndefined(data.endDate)) {
            result.endDate = data.endDate;
        }
        if (notNullOrUndefined(data.dateOrRange)) {
            result.dateOrRange = data.dateOrRange;
        } else {
            result.dateOrRange = DateRange.create({});
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "StaffLevel"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    staffAvailable!: string; // implementation of staffAvailable
    startDate!: DateConcept; // implementation of part 'startDate'
    endDate: DateConcept | undefined; // implementation of part 'endDate'
    dateOrRange!: DateRange; // implementation of part 'dateOrRange'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "staffAvailable");
        this.staffAvailable = "";

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "startDate");

        observablepart(this, "endDate");

        observablepart(this, "dateOrRange");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): StaffLevel {
        const result = new StaffLevel();
        if (notNullOrUndefined(this.staffAvailable)) {
            result.staffAvailable = this.staffAvailable;
        }
        if (notNullOrUndefined(this.startDate)) {
            result.startDate = this.startDate.copy();
        }
        if (notNullOrUndefined(this.endDate)) {
            result.endDate = this.endDate.copy();
        }
        if (notNullOrUndefined(this.dateOrRange)) {
            result.dateOrRange = this.dateOrRange.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<StaffLevel>): boolean {
        let result: boolean = true;
        if (
            result &&
            toBeMatched.staffAvailable !== null &&
            toBeMatched.staffAvailable !== undefined &&
            toBeMatched.staffAvailable.length > 0
        ) {
            result = result && this.staffAvailable === toBeMatched.staffAvailable;
        }
        if (result && notNullOrUndefined(toBeMatched.startDate)) {
            result = result && this.startDate.match(toBeMatched.startDate);
        }
        if (result && notNullOrUndefined(toBeMatched.endDate)) {
            if (this.endDate) {
                result = result && this.endDate.match(toBeMatched.endDate);
            }
        }
        if (result && notNullOrUndefined(toBeMatched.dateOrRange)) {
            result = result && this.dateOrRange.match(toBeMatched.dateOrRange);
        }
        return result;
    }
}
