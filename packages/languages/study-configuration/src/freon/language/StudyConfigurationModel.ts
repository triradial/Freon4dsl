// Generated by the Freon Language Generator.
import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    type FreModel,
    FreLanguage,
    FreParseLocation,
    type FreModelUnit,
    AST,
    notNullOrUndefined,
    FreUtils,
} from "@freon4dsl/core";
import { StudyConfiguration, Availability, PatientInfo, PatientHistoryUnit } from "./internal.js";

import { makeObservable, action, runInAction } from "mobx";

/**
 * Class StudyConfigurationModel is the implementation of the model with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class StudyConfigurationModel extends MobxModelElementImpl implements FreModel {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<StudyConfigurationModel>): StudyConfigurationModel {
        const result = new StudyConfigurationModel(data.$id);
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.configuration)) {
            result.configuration = data.configuration;
        }
        if (notNullOrUndefined(data.availability)) {
            result.availability = data.availability;
        }
        if (notNullOrUndefined(data.patients)) {
            result.patients = data.patients;
        }
        if (notNullOrUndefined(data.currentPatient)) {
            result.currentPatient = data.currentPatient;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "StudyConfigurationModel"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    name!: string; // implementation of name
    configuration!: StudyConfiguration; // implementation of part 'configuration'
    availability!: Availability; // implementation of part 'availability'
    patients!: PatientInfo; // implementation of part 'patients'
    currentPatient!: PatientHistoryUnit; // implementation of part 'currentPatient'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "configuration");

        observablepart(this, "availability");

        observablepart(this, "patients");

        observablepart(this, "currentPatient");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): StudyConfigurationModel {
        const result = new StudyConfigurationModel();
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.configuration)) {
            result.configuration = this.configuration.copy();
        }
        if (notNullOrUndefined(this.availability)) {
            result.availability = this.availability.copy();
        }
        if (notNullOrUndefined(this.patients)) {
            result.patients = this.patients.copy();
        }
        if (notNullOrUndefined(this.currentPatient)) {
            result.currentPatient = this.currentPatient.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<StudyConfigurationModel>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && notNullOrUndefined(toBeMatched.configuration)) {
            result = result && this.configuration.match(toBeMatched.configuration);
        }
        if (result && notNullOrUndefined(toBeMatched.availability)) {
            result = result && this.availability.match(toBeMatched.availability);
        }
        if (result && notNullOrUndefined(toBeMatched.patients)) {
            result = result && this.patients.match(toBeMatched.patients);
        }
        if (result && notNullOrUndefined(toBeMatched.currentPatient)) {
            result = result && this.currentPatient.match(toBeMatched.currentPatient);
        }
        return result;
    }

    /**
     * A convenience method that finds a unit of this model based on its name and 'metatype'.
     * @param name
     * @param metatype
     */
    findUnit(name: string, metatype?: string): FreModelUnit | undefined {
        let result: FreModelUnit | undefined;
        const checkType = metatype !== undefined;
        result = this.getUnits().find(
            (mod) => mod.name === name && (checkType ? FreLanguage.getInstance().metaConformsToType(mod, metatype) : true),
        );
        if (!!result) {
            return result;
        }
        return undefined;
    }

    /**
     * Adds a model unit. Returns false if anything goes wrong.
     *
     * @param newUnit
     */
    addUnit(newUnit: FreModelUnit): boolean {
        if (!!newUnit) {
            const myMetatype = newUnit.freLanguageConcept();
            switch (myMetatype) {
                case "StudyConfiguration": {
                    AST.changeNamed("addUnit", () => {
                        this.configuration = newUnit as StudyConfiguration;
                    });
                    return true;
                }
                case "Availability": {
                    AST.changeNamed("addUnit", () => {
                        this.availability = newUnit as Availability;
                    });
                    return true;
                }
                case "PatientInfo": {
                    AST.changeNamed("addUnit", () => {
                        this.patients = newUnit as PatientInfo;
                    });
                    return true;
                }
                case "PatientHistoryUnit": {
                    AST.changeNamed("addUnit", () => {
                        this.currentPatient = newUnit as PatientHistoryUnit;
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Removes a model unit. Returns false if anything goes wrong.
     *
     * @param oldUnit
     */
    removeUnit(oldUnit: FreModelUnit): boolean {
        if (!!oldUnit) {
            const myMetatype = oldUnit.freLanguageConcept();
            switch (myMetatype) {
                case "StudyConfiguration": {
                    AST.changeNamed("removeUnit", () => {
                        // @ts-ignore: we want to keep the type of 'unit' clear, thus we ignore the compiler error
                        this.configuration = null;
                    });
                    return true;
                }
                case "Availability": {
                    AST.changeNamed("removeUnit", () => {
                        // @ts-ignore: we want to keep the type of 'unit' clear, thus we ignore the compiler error
                        this.availability = null;
                    });
                    return true;
                }
                case "PatientInfo": {
                    AST.changeNamed("removeUnit", () => {
                        // @ts-ignore: we want to keep the type of 'unit' clear, thus we ignore the compiler error
                        this.patients = null;
                    });
                    return true;
                }
                case "PatientHistoryUnit": {
                    AST.changeNamed("removeUnit", () => {
                        // @ts-ignore: we want to keep the type of 'unit' clear, thus we ignore the compiler error
                        this.currentPatient = null;
                    });
                    return true;
                }
            }
        }
        return false;
    }

    /**
     * Returns an empty model unit of type 'typeName' within 'model'.
     * Returns undefined when the given 'typename' does not match any of the unit types
     * known in the language.
     *
     * @param typename
     */
    newUnit(typename: string): FreModelUnit | undefined {
        switch (typename) {
            case "StudyConfiguration": {
                let unit: StudyConfiguration | undefined;
                runInAction(() => {
                    unit = StudyConfiguration.create({});
                });
                if (!unit) throw new Error("Failed to create StudyConfiguration");
                AST.changeNamed("newUnit", () => {
                    this.configuration = unit as StudyConfiguration;
                });
                return unit;
            }
            case "Availability": {
                let unit: Availability | undefined;
                runInAction(() => {
                    unit = Availability.create({});
                });
                if (!unit) throw new Error("Failed to create Availability");
                AST.changeNamed("newUnit", () => {
                    this.availability = unit as Availability;
                });
                return unit;
            }
            case "PatientInfo": {
                let unit: PatientInfo | undefined;
                runInAction(() => {
                    unit = PatientInfo.create({});
                });
                if (!unit) throw new Error("Failed to create PatientInfo");
                AST.changeNamed("newUnit", () => {
                    this.patients = unit as PatientInfo;
                });
                return unit;
            }
            case "PatientHistoryUnit": {
                let unit: PatientHistoryUnit | undefined;
                runInAction(() => {
                    unit = PatientHistoryUnit.create({});
                });
                if (!unit) throw new Error("Failed to create PatientHistoryUnit");
                AST.changeNamed("newUnit", () => {
                    this.currentPatient = unit as PatientHistoryUnit;
                });
                return unit;
            }
        }
        return undefined;
    }

    /**
     * Returns a list of model units.
     */
    getUnits(): FreModelUnit[] {
        let result: FreModelUnit[] = [];
        if (!!this.configuration) {
            result.push(this.configuration);
        }
        if (!!this.availability) {
            result.push(this.availability);
        }
        if (!!this.patients) {
            result.push(this.patients);
        }
        if (!!this.currentPatient) {
            result.push(this.currentPatient);
        }
        return result;
    }

    /**
     * Returns a list of model units of type 'type'.
     */
    getUnitsForType(type: string): FreModelUnit[] {
        switch (type) {
            case "StudyConfiguration": {
                let result: FreModelUnit[] = [];
                result.push(this.configuration);
                return result;
            }
            case "Availability": {
                let result: FreModelUnit[] = [];
                result.push(this.availability);
                return result;
            }
            case "PatientInfo": {
                let result: FreModelUnit[] = [];
                result.push(this.patients);
                return result;
            }
            case "PatientHistoryUnit": {
                let result: FreModelUnit[] = [];
                result.push(this.currentPatient);
                return result;
            }
        }
        return [];
    }
}
