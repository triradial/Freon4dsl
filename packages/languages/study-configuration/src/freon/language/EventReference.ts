// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observablepart,
    type FreNode,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
    FreUtils,
} from "@freon4dsl/core";
import { Event, EventState } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class EventReference is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class EventReference extends MobxModelElementImpl implements FreNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<EventReference>): EventReference {
        const result = new EventReference(data.$id);
        if (notNullOrUndefined(data.event)) {
            result.event = data.event;
        }
        if (notNullOrUndefined(data.eventState)) {
            result.eventState = data.eventState;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "EventReference"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed

    event!: FreNodeReference<Event>; // implementation of reference 'event'
    eventState!: FreNodeReference<EventState>; // implementation of reference 'eventState'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "event");

        observablepart(this, "eventState");
        this.eventState = FreNodeReference.create<EventState>(EventState.completed, "EventState");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): EventReference {
        const result = new EventReference();
        if (notNullOrUndefined(this.event)) {
            result.event = this.event.copy();
        }
        if (notNullOrUndefined(this.eventState)) {
            result.eventState = this.eventState.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<EventReference>): boolean {
        let result: boolean = true;
        if (result && notNullOrUndefined(toBeMatched.event)) {
            result = result && this.event.match(toBeMatched.event);
        }
        if (result && notNullOrUndefined(toBeMatched.eventState)) {
            result = result && this.eventState.match(toBeMatched.eventState);
        }
        return result;
    }

    /**
     * Convenience method for reference 'event'.
     * Instead of returning a 'FreNodeReference<Event>' object,
     * it returns the referred 'Event' object, if it can be found.
     */
    get $event(): Event | undefined {
        if (!!this.event) {
            return this.event.referred;
        }
        return undefined;
    }
    /**
     * Convenience method for reference 'eventState'.
     * Instead of returning a 'FreNodeReference<EventState>' object,
     * it returns the referred 'EventState' object, if it can be found.
     */
    get $eventState(): EventState | undefined {
        if (!!this.eventState) {
            return this.eventState.referred;
        }
        return undefined;
    }
}
