// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import { observablepart, type FreExpressionNode, FreParseLocation, notNullOrUndefined, FreNodeReference } from "@freon4dsl/core";
import { ScheduleExpression, Period, PhaseState } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate

/**
 * Class PeriodReferenceExpression is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class PeriodReferenceExpression extends ScheduleExpression implements FreExpressionNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<PeriodReferenceExpression>): PeriodReferenceExpression {
        const result = new PeriodReferenceExpression(data.$id);
        if (notNullOrUndefined(data.period)) {
            result.period = data.period;
        }
        if (notNullOrUndefined(data.periodState)) {
            result.periodState = data.periodState;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "PeriodReferenceExpression"; // holds the metatype in the form of a string

    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed

    period!: FreNodeReference<Period>; // implementation of reference 'period'
    periodState!: FreNodeReference<PhaseState>; // implementation of reference 'periodState'

    constructor(id?: string) {
        super(id);

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepart(this, "period");

        observablepart(this, "periodState");
        this.periodState = FreNodeReference.create<PhaseState>(PhaseState.completed, "PhaseState");
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): PeriodReferenceExpression {
        const result = new PeriodReferenceExpression();
        if (notNullOrUndefined(this.period)) {
            result.period = this.period.copy();
        }
        if (notNullOrUndefined(this.periodState)) {
            result.periodState = this.periodState.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<PeriodReferenceExpression>): boolean {
        let result: boolean = super.match(toBeMatched);
        if (result && notNullOrUndefined(toBeMatched.period)) {
            result = result && this.period.match(toBeMatched.period);
        }
        if (result && notNullOrUndefined(toBeMatched.periodState)) {
            result = result && this.periodState.match(toBeMatched.periodState);
        }
        return result;
    }

    /**
     * Convenience method for reference 'period'.
     * Instead of returning a 'FreNodeReference<Period>' object,
     * it returns the referred 'Period' object, if it can be found.
     */
    get $period(): Period | undefined {
        if (!!this.period) {
            return this.period.referred;
        }
        return undefined;
    }
    /**
     * Convenience method for reference 'periodState'.
     * Instead of returning a 'FreNodeReference<PhaseState>' object,
     * it returns the referred 'PhaseState' object, if it can be found.
     */
    get $periodState(): PhaseState | undefined {
        if (!!this.periodState) {
            return this.periodState.referred;
        }
        return undefined;
    }
}
