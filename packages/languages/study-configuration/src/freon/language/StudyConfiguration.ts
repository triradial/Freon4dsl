// Generated by the Freon Language Generator.
// TEMPLATE: UnitTemplate.generateUnit(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    observablepartlist,
    type FreModelUnit,
    FreParseLocation,
    notNullOrUndefined,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { Period, SharedTask, SystemAccess, Person, StaffRole, Description } from "./internal.js";

import { makeObservable, action } from "mobx";

/**
 * Class StudyConfiguration is the implementation of the model unit with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to any changes in the state of its properties.
 */
export class StudyConfiguration extends MobxModelElementImpl implements FreModelUnit {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<StudyConfiguration>): StudyConfiguration {
        const result = new StudyConfiguration(data.$id);
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.showSharedTasks)) {
            result.showSharedTasks = data.showSharedTasks;
        }
        if (notNullOrUndefined(data.showScheduling)) {
            result.showScheduling = data.showScheduling;
        }
        if (notNullOrUndefined(data.showSchedulingDetails)) {
            result.showSchedulingDetails = data.showSchedulingDetails;
        }
        if (notNullOrUndefined(data.showChecklists)) {
            result.showChecklists = data.showChecklists;
        }
        if (notNullOrUndefined(data.showSteps)) {
            result.showSteps = data.showSteps;
        }
        if (notNullOrUndefined(data.showReferences)) {
            result.showReferences = data.showReferences;
        }
        if (notNullOrUndefined(data.showSystems)) {
            result.showSystems = data.showSystems;
        }
        if (notNullOrUndefined(data.showPeople)) {
            result.showPeople = data.showPeople;
        }
        if (notNullOrUndefined(data.showDescriptions)) {
            result.showDescriptions = data.showDescriptions;
        }
        if (notNullOrUndefined(data.studyStartDayNumber)) {
            result.studyStartDayNumber = data.studyStartDayNumber;
        }
        if (notNullOrUndefined(data.periods)) {
            data.periods.forEach((x) => result.periods.push(x));
        }
        if (notNullOrUndefined(data.tasks)) {
            data.tasks.forEach((x) => result.tasks.push(x));
        }
        if (notNullOrUndefined(data.systemAccesses)) {
            data.systemAccesses.forEach((x) => result.systemAccesses.push(x));
        }
        if (notNullOrUndefined(data.staffing)) {
            data.staffing.forEach((x) => result.staffing.push(x));
        }
        if (notNullOrUndefined(data.roles)) {
            data.roles.forEach((x) => result.roles.push(x));
        }
        if (notNullOrUndefined(data.description)) {
            result.description = data.description;
        } else {
            result.description = Description.create({});
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    fileExtension: string = "";
    readonly $typename: string = "StudyConfiguration"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    name!: string; // implementation of name
    showSharedTasks!: boolean; // implementation of showSharedTasks
    showScheduling!: boolean; // implementation of showScheduling
    showSchedulingDetails!: boolean; // implementation of showSchedulingDetails
    showChecklists!: boolean; // implementation of showChecklists
    showSteps!: boolean; // implementation of showSteps
    showReferences!: boolean; // implementation of showReferences
    showSystems!: boolean; // implementation of showSystems
    showPeople!: boolean; // implementation of showPeople
    showDescriptions!: boolean; // implementation of showDescriptions
    studyStartDayNumber!: number; // implementation of studyStartDayNumber
    periods!: Period[]; // implementation of part 'periods'
    tasks!: SharedTask[]; // implementation of part 'tasks'
    systemAccesses!: SystemAccess[]; // implementation of part 'systemAccesses'
    staffing!: Person[]; // implementation of part 'staffing'
    roles!: StaffRole[]; // implementation of part 'roles'
    description!: Description; // implementation of part 'description'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "Study Configuration";
        observableprim(this, "showSharedTasks");
        this.showSharedTasks = false;
        observableprim(this, "showScheduling");
        this.showScheduling = true;
        observableprim(this, "showSchedulingDetails");
        this.showSchedulingDetails = false;
        observableprim(this, "showChecklists");
        this.showChecklists = false;
        observableprim(this, "showSteps");
        this.showSteps = true;
        observableprim(this, "showReferences");
        this.showReferences = false;
        observableprim(this, "showSystems");
        this.showSystems = false;
        observableprim(this, "showPeople");
        this.showPeople = false;
        observableprim(this, "showDescriptions");
        this.showDescriptions = true;
        observableprim(this, "studyStartDayNumber");
        this.studyStartDayNumber = 0;

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "periods");
        observablepartlist(this, "tasks");
        observablepartlist(this, "systemAccesses");
        observablepartlist(this, "staffing");
        observablepartlist(this, "roles");
        observablepart(this, "description");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return true;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): StudyConfiguration {
        const result = new StudyConfiguration();
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.showSharedTasks)) {
            result.showSharedTasks = this.showSharedTasks;
        }
        if (notNullOrUndefined(this.showScheduling)) {
            result.showScheduling = this.showScheduling;
        }
        if (notNullOrUndefined(this.showSchedulingDetails)) {
            result.showSchedulingDetails = this.showSchedulingDetails;
        }
        if (notNullOrUndefined(this.showChecklists)) {
            result.showChecklists = this.showChecklists;
        }
        if (notNullOrUndefined(this.showSteps)) {
            result.showSteps = this.showSteps;
        }
        if (notNullOrUndefined(this.showReferences)) {
            result.showReferences = this.showReferences;
        }
        if (notNullOrUndefined(this.showSystems)) {
            result.showSystems = this.showSystems;
        }
        if (notNullOrUndefined(this.showPeople)) {
            result.showPeople = this.showPeople;
        }
        if (notNullOrUndefined(this.showDescriptions)) {
            result.showDescriptions = this.showDescriptions;
        }
        if (notNullOrUndefined(this.studyStartDayNumber)) {
            result.studyStartDayNumber = this.studyStartDayNumber;
        }
        if (notNullOrUndefined(this.periods)) {
            this.periods.forEach((x) => result.periods.push(x.copy()));
        }
        if (notNullOrUndefined(this.tasks)) {
            this.tasks.forEach((x) => result.tasks.push(x.copy()));
        }
        if (notNullOrUndefined(this.systemAccesses)) {
            this.systemAccesses.forEach((x) => result.systemAccesses.push(x.copy()));
        }
        if (notNullOrUndefined(this.staffing)) {
            this.staffing.forEach((x) => result.staffing.push(x.copy()));
        }
        if (notNullOrUndefined(this.roles)) {
            this.roles.forEach((x) => result.roles.push(x.copy()));
        }
        if (notNullOrUndefined(this.description)) {
            result.description = this.description.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<StudyConfiguration>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (result && toBeMatched.showSharedTasks !== null && toBeMatched.showSharedTasks !== undefined) {
            result = result && this.showSharedTasks === toBeMatched.showSharedTasks;
        }
        if (result && toBeMatched.showScheduling !== null && toBeMatched.showScheduling !== undefined) {
            result = result && this.showScheduling === toBeMatched.showScheduling;
        }
        if (result && toBeMatched.showSchedulingDetails !== null && toBeMatched.showSchedulingDetails !== undefined) {
            result = result && this.showSchedulingDetails === toBeMatched.showSchedulingDetails;
        }
        if (result && toBeMatched.showChecklists !== null && toBeMatched.showChecklists !== undefined) {
            result = result && this.showChecklists === toBeMatched.showChecklists;
        }
        if (result && toBeMatched.showSteps !== null && toBeMatched.showSteps !== undefined) {
            result = result && this.showSteps === toBeMatched.showSteps;
        }
        if (result && toBeMatched.showReferences !== null && toBeMatched.showReferences !== undefined) {
            result = result && this.showReferences === toBeMatched.showReferences;
        }
        if (result && toBeMatched.showSystems !== null && toBeMatched.showSystems !== undefined) {
            result = result && this.showSystems === toBeMatched.showSystems;
        }
        if (result && toBeMatched.showPeople !== null && toBeMatched.showPeople !== undefined) {
            result = result && this.showPeople === toBeMatched.showPeople;
        }
        if (result && toBeMatched.showDescriptions !== null && toBeMatched.showDescriptions !== undefined) {
            result = result && this.showDescriptions === toBeMatched.showDescriptions;
        }
        if (result && toBeMatched.studyStartDayNumber !== null && toBeMatched.studyStartDayNumber !== undefined) {
            result = result && this.studyStartDayNumber === toBeMatched.studyStartDayNumber;
        }
        if (result && notNullOrUndefined(toBeMatched.periods)) {
            result = result && matchElementList(this.periods, toBeMatched.periods);
        }
        if (result && notNullOrUndefined(toBeMatched.tasks)) {
            result = result && matchElementList(this.tasks, toBeMatched.tasks);
        }
        if (result && notNullOrUndefined(toBeMatched.systemAccesses)) {
            result = result && matchElementList(this.systemAccesses, toBeMatched.systemAccesses);
        }
        if (result && notNullOrUndefined(toBeMatched.staffing)) {
            result = result && matchElementList(this.staffing, toBeMatched.staffing);
        }
        if (result && notNullOrUndefined(toBeMatched.roles)) {
            result = result && matchElementList(this.roles, toBeMatched.roles);
        }
        if (result && notNullOrUndefined(toBeMatched.description)) {
            result = result && this.description.match(toBeMatched.description);
        }
        return result;
    }
}
