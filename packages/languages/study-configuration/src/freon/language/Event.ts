// Generated by the Freon Language Generator.
// TEMPLATE ConceptTemplate.generateConceptPrivate(...)

import {
    MobxModelElementImpl,
    observableprim,
    observablepart,
    observablepartlist,
    type FreNamedNode,
    FreParseLocation,
    notNullOrUndefined,
    FreNodeReference,
    FreUtils,
    matchElementList,
} from "@freon4dsl/core";
import { EventTask, Description, EventSchedule, TypeOfEvent } from "./internal.js";

// TEMPLATE: ConceptTemplate.generateConceptPrivate
import { makeObservable, action } from "mobx";

/**
 * Class Event is the implementation of the concept with the same name in the language definition file.
 * It uses mobx decorators to enable parts of the language environment, e.g. the editor, to react
 * to the changes in the state of its properties.
 */
export class Event extends MobxModelElementImpl implements FreNamedNode {
    /**
     * A convenience method that creates an instance of this class
     * based on the properties defined in 'data'.
     * @param data partial object
     */
    static create(data: Partial<Event>): Event {
        const result = new Event(data.$id);
        if (notNullOrUndefined(data.name)) {
            result.name = data.name;
        }
        if (notNullOrUndefined(data.alternativeName)) {
            result.alternativeName = data.alternativeName;
        }
        if (notNullOrUndefined(data.showSequenced)) {
            result.showSequenced = data.showSequenced;
        }
        if (notNullOrUndefined(data.eventNumber)) {
            result.eventNumber = data.eventNumber;
        }
        if (notNullOrUndefined(data.tasks)) {
            data.tasks.forEach((x) => result.tasks.push(x));
        }
        if (notNullOrUndefined(data.description)) {
            result.description = data.description;
        } else {
            result.description = Description.create({});
        }
        if (notNullOrUndefined(data.schedule)) {
            result.schedule = data.schedule;
        } else {
            result.schedule = EventSchedule.create({});
        }
        if (notNullOrUndefined(data.typeOfEvent)) {
            result.typeOfEvent = data.typeOfEvent;
        }
        if (notNullOrUndefined(data.parseLocation)) {
            result.parseLocation = data.parseLocation;
        }
        return result;
    }

    readonly $typename: string = "Event"; // holds the metatype in the form of a string
    $id: string = ""; // a unique identifier
    parseLocation: FreParseLocation | undefined; // if relevant, the location of this element within the source from which it is parsed
    name!: string; // implementation of name
    alternativeName!: string; // implementation of alternativeName
    showSequenced!: boolean; // implementation of showSequenced
    eventNumber!: number; // implementation of eventNumber
    tasks!: EventTask[]; // implementation of part 'tasks'
    description!: Description; // implementation of part 'description'
    schedule!: EventSchedule; // implementation of part 'schedule'
    typeOfEvent!: FreNodeReference<TypeOfEvent>; // implementation of reference 'typeOfEvent'

    constructor(id?: string) {
        super();
        if (!!id) {
            this.$id = id;
        } else {
            this.$id = FreUtils.ID(); // uuid.v4();
        }
        // Both 'observableprim' and 'observableprimlist' change the get and set of the attribute
        // such that the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observableprim(this, "name");
        this.name = "";
        observableprim(this, "alternativeName");
        this.alternativeName = "";
        observableprim(this, "showSequenced");
        this.showSequenced = false;
        observableprim(this, "eventNumber");
        this.eventNumber = 0;

        // Both 'observablepart' and 'observablepartlist' change the get and set of the attribute
        // such that the parent-part relationship is consistently maintained,
        // and make sure the part is observable. In lists no 'null' or 'undefined' values are allowed.
        observablepartlist(this, "tasks");
        observablepart(this, "description");

        observablepart(this, "schedule");

        observablepart(this, "typeOfEvent");
        this.typeOfEvent = FreNodeReference.create<TypeOfEvent>(TypeOfEvent.site, "TypeOfEvent");

        // Make copy method a mobx action
        makeObservable(this, {
            copy: action,
        });
    }

    /**
     * Returns the metatype of this instance in the form of a string.
     */
    freLanguageConcept(): string {
        return this.$typename;
    }

    /**
     * Returns the unique identifier of this instance.
     */
    freId(): string {
        return this.$id;
    }

    /**
     * Returns true if this instance is a model concept.
     */
    freIsModel(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a model unit.
     */
    freIsUnit(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is an expression concept.
     */
    freIsExpression(): boolean {
        return false;
    }

    /**
     * Returns true if this instance is a binary expression concept.
     */
    freIsBinaryExpression(): boolean {
        return false;
    }
    /**
     * A convenience method that copies this instance into a new object.
     */
    copy(): Event {
        const result = new Event();
        if (notNullOrUndefined(this.name)) {
            result.name = this.name;
        }
        if (notNullOrUndefined(this.alternativeName)) {
            result.alternativeName = this.alternativeName;
        }
        if (notNullOrUndefined(this.showSequenced)) {
            result.showSequenced = this.showSequenced;
        }
        if (notNullOrUndefined(this.eventNumber)) {
            result.eventNumber = this.eventNumber;
        }
        if (notNullOrUndefined(this.tasks)) {
            this.tasks.forEach((x) => result.tasks.push(x.copy()));
        }
        if (notNullOrUndefined(this.description)) {
            result.description = this.description.copy();
        }
        if (notNullOrUndefined(this.schedule)) {
            result.schedule = this.schedule.copy();
        }
        if (notNullOrUndefined(this.typeOfEvent)) {
            result.typeOfEvent = this.typeOfEvent.copy();
        }
        return result;
    }
    /**
     * Matches a partial instance of this class to this object
     * based on the properties defined in the partial.
     * @param toBeMatched
     */
    public match(toBeMatched: Partial<Event>): boolean {
        let result: boolean = true;
        if (result && toBeMatched.name !== null && toBeMatched.name !== undefined && toBeMatched.name.length > 0) {
            result = result && this.name === toBeMatched.name;
        }
        if (
            result &&
            toBeMatched.alternativeName !== null &&
            toBeMatched.alternativeName !== undefined &&
            toBeMatched.alternativeName.length > 0
        ) {
            result = result && this.alternativeName === toBeMatched.alternativeName;
        }
        if (result && toBeMatched.showSequenced !== null && toBeMatched.showSequenced !== undefined) {
            result = result && this.showSequenced === toBeMatched.showSequenced;
        }
        if (result && toBeMatched.eventNumber !== null && toBeMatched.eventNumber !== undefined) {
            result = result && this.eventNumber === toBeMatched.eventNumber;
        }
        if (result && notNullOrUndefined(toBeMatched.tasks)) {
            result = result && matchElementList(this.tasks, toBeMatched.tasks);
        }
        if (result && notNullOrUndefined(toBeMatched.description)) {
            result = result && this.description.match(toBeMatched.description);
        }
        if (result && notNullOrUndefined(toBeMatched.schedule)) {
            result = result && this.schedule.match(toBeMatched.schedule);
        }
        if (result && notNullOrUndefined(toBeMatched.typeOfEvent)) {
            result = result && this.typeOfEvent.match(toBeMatched.typeOfEvent);
        }
        return result;
    }

    /**
     * Convenience method for reference 'typeOfEvent'.
     * Instead of returning a 'FreNodeReference<TypeOfEvent>' object,
     * it returns the referred 'TypeOfEvent' object, if it can be found.
     */
    get $typeOfEvent(): TypeOfEvent | undefined {
        if (!!this.typeOfEvent) {
            return this.typeOfEvent.referred;
        }
        return undefined;
    }
}
