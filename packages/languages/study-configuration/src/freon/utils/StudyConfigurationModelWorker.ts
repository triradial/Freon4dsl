// Generated by the Freon Language Generator.
// TEMPLATE: WorkerInterfaceTemplate.generateWorkerInterface(...)

import {
    ScheduleExpression,
    EventReferenceExpression,
    PeriodReferenceExpression,
    Time,
    PreviousVisit,
    StartDay,
    CountExpression,
    LiteralExpression,
    String,
    NumberLiteralExpression,
    BinaryExpression,
    MultiplyExpression,
    PlusExpression,
    MinusExpression,
    DivideExpression,
    AndExpression,
    OrExpression,
    ComparisonExpression,
    LessThenExpression,
    GreaterThenExpression,
    EqualsExpression,
    TimeExpression,
    PatientHistory,
    PatientVisitStatus,
    PatientVisit,
    PatientNotAvailable,
    StaffLevel,
    Month,
    VisitDate,
    DateRange,
    DateConcept,
    EventSchedule,
    EventReference,
    TimeUnit,
    PhaseState,
    EventState,
    SimpleOperators,
    Days,
    TimeAmount,
    TimeAmountPart,
    Day,
    When,
    StudyStart,
    FirstDayOfStudy,
    Baseline,
    Previous,
    Unscheduled,
    AnyDay,
    EventWindow,
    ComplianceWindow,
    ComplianceWindowOf,
    StartingBy,
    EndingBy,
    BetweenTimes,
    RepeatCondition,
    Daily,
    Weekly,
    Monthly,
    Forever,
    RepeatEvery,
    RepeatCount,
    Period,
    TypeOfEvent,
    Event,
    AbstractTask,
    EventTask,
    Task,
    SharedTask,
    TaskReference,
    Step,
    WorkflowDescription,
    WorkflowBranch,
    Description,
    AbstractSystemAccess,
    SystemAccess,
    SystemAccessReference,
    AccessedAtURL,
    AccessedAPhoneNumber,
    StaffRole,
    AbstractPerson,
    PersonReference,
    Person,
    Reference,
    RobotMapping,
    SourceToTargetMapping,
    Availability,
    PatientInfo,
    PatientHistoryUnit,
    StudyConfiguration,
    StudyConfigurationModel,
} from "../language/index.js";

/**
 * Interface StudyConfigurationModelWorker implements the extended visitor pattern of instances of language StudyConfigurationModel.
 * Class StudyConfigurationModelWalker implements the traversal of the model tree, classes that implement this interface
 * are responsible for the actual work being done on the nodes of the tree.
 * Every node is visited twice, once before the visit of its children, and once after this visit.
 */
export interface StudyConfigurationModelWorker {
    execBeforeStudyConfigurationModel(node: StudyConfigurationModel): boolean;
    execAfterStudyConfigurationModel(node: StudyConfigurationModel): boolean;

    execBeforeAvailability(node: Availability): boolean;
    execAfterAvailability(node: Availability): boolean;

    execBeforePatientInfo(node: PatientInfo): boolean;
    execAfterPatientInfo(node: PatientInfo): boolean;

    execBeforePatientHistoryUnit(node: PatientHistoryUnit): boolean;
    execAfterPatientHistoryUnit(node: PatientHistoryUnit): boolean;

    execBeforeStudyConfiguration(node: StudyConfiguration): boolean;
    execAfterStudyConfiguration(node: StudyConfiguration): boolean;

    execBeforeScheduleExpression(node: ScheduleExpression): boolean;
    execAfterScheduleExpression(node: ScheduleExpression): boolean;

    execBeforeEventReferenceExpression(node: EventReferenceExpression): boolean;
    execAfterEventReferenceExpression(node: EventReferenceExpression): boolean;

    execBeforePeriodReferenceExpression(node: PeriodReferenceExpression): boolean;
    execAfterPeriodReferenceExpression(node: PeriodReferenceExpression): boolean;

    execBeforeTime(node: Time): boolean;
    execAfterTime(node: Time): boolean;

    execBeforePreviousVisit(node: PreviousVisit): boolean;
    execAfterPreviousVisit(node: PreviousVisit): boolean;

    execBeforeStartDay(node: StartDay): boolean;
    execAfterStartDay(node: StartDay): boolean;

    execBeforeCountExpression(node: CountExpression): boolean;
    execAfterCountExpression(node: CountExpression): boolean;

    execBeforeLiteralExpression(node: LiteralExpression): boolean;
    execAfterLiteralExpression(node: LiteralExpression): boolean;

    execBeforeString(node: String): boolean;
    execAfterString(node: String): boolean;

    execBeforeNumberLiteralExpression(node: NumberLiteralExpression): boolean;
    execAfterNumberLiteralExpression(node: NumberLiteralExpression): boolean;

    execBeforeBinaryExpression(node: BinaryExpression): boolean;
    execAfterBinaryExpression(node: BinaryExpression): boolean;

    execBeforeMultiplyExpression(node: MultiplyExpression): boolean;
    execAfterMultiplyExpression(node: MultiplyExpression): boolean;

    execBeforePlusExpression(node: PlusExpression): boolean;
    execAfterPlusExpression(node: PlusExpression): boolean;

    execBeforeMinusExpression(node: MinusExpression): boolean;
    execAfterMinusExpression(node: MinusExpression): boolean;

    execBeforeDivideExpression(node: DivideExpression): boolean;
    execAfterDivideExpression(node: DivideExpression): boolean;

    execBeforeAndExpression(node: AndExpression): boolean;
    execAfterAndExpression(node: AndExpression): boolean;

    execBeforeOrExpression(node: OrExpression): boolean;
    execAfterOrExpression(node: OrExpression): boolean;

    execBeforeComparisonExpression(node: ComparisonExpression): boolean;
    execAfterComparisonExpression(node: ComparisonExpression): boolean;

    execBeforeLessThenExpression(node: LessThenExpression): boolean;
    execAfterLessThenExpression(node: LessThenExpression): boolean;

    execBeforeGreaterThenExpression(node: GreaterThenExpression): boolean;
    execAfterGreaterThenExpression(node: GreaterThenExpression): boolean;

    execBeforeEqualsExpression(node: EqualsExpression): boolean;
    execAfterEqualsExpression(node: EqualsExpression): boolean;

    execBeforeTimeExpression(node: TimeExpression): boolean;
    execAfterTimeExpression(node: TimeExpression): boolean;

    execBeforePatientHistory(node: PatientHistory): boolean;
    execAfterPatientHistory(node: PatientHistory): boolean;

    execBeforePatientVisitStatus(node: PatientVisitStatus): boolean;
    execAfterPatientVisitStatus(node: PatientVisitStatus): boolean;

    execBeforePatientVisit(node: PatientVisit): boolean;
    execAfterPatientVisit(node: PatientVisit): boolean;

    execBeforePatientNotAvailable(node: PatientNotAvailable): boolean;
    execAfterPatientNotAvailable(node: PatientNotAvailable): boolean;

    execBeforeStaffLevel(node: StaffLevel): boolean;
    execAfterStaffLevel(node: StaffLevel): boolean;

    execBeforeMonth(node: Month): boolean;
    execAfterMonth(node: Month): boolean;

    execBeforeVisitDate(node: VisitDate): boolean;
    execAfterVisitDate(node: VisitDate): boolean;

    execBeforeDateRange(node: DateRange): boolean;
    execAfterDateRange(node: DateRange): boolean;

    execBeforeDateConcept(node: DateConcept): boolean;
    execAfterDateConcept(node: DateConcept): boolean;

    execBeforeEventSchedule(node: EventSchedule): boolean;
    execAfterEventSchedule(node: EventSchedule): boolean;

    execBeforeEventReference(node: EventReference): boolean;
    execAfterEventReference(node: EventReference): boolean;

    execBeforeTimeUnit(node: TimeUnit): boolean;
    execAfterTimeUnit(node: TimeUnit): boolean;

    execBeforePhaseState(node: PhaseState): boolean;
    execAfterPhaseState(node: PhaseState): boolean;

    execBeforeEventState(node: EventState): boolean;
    execAfterEventState(node: EventState): boolean;

    execBeforeSimpleOperators(node: SimpleOperators): boolean;
    execAfterSimpleOperators(node: SimpleOperators): boolean;

    execBeforeDays(node: Days): boolean;
    execAfterDays(node: Days): boolean;

    execBeforeTimeAmount(node: TimeAmount): boolean;
    execAfterTimeAmount(node: TimeAmount): boolean;

    execBeforeTimeAmountPart(node: TimeAmountPart): boolean;
    execAfterTimeAmountPart(node: TimeAmountPart): boolean;

    execBeforeDay(node: Day): boolean;
    execAfterDay(node: Day): boolean;

    execBeforeWhen(node: When): boolean;
    execAfterWhen(node: When): boolean;

    execBeforeStudyStart(node: StudyStart): boolean;
    execAfterStudyStart(node: StudyStart): boolean;

    execBeforeFirstDayOfStudy(node: FirstDayOfStudy): boolean;
    execAfterFirstDayOfStudy(node: FirstDayOfStudy): boolean;

    execBeforeBaseline(node: Baseline): boolean;
    execAfterBaseline(node: Baseline): boolean;

    execBeforePrevious(node: Previous): boolean;
    execAfterPrevious(node: Previous): boolean;

    execBeforeUnscheduled(node: Unscheduled): boolean;
    execAfterUnscheduled(node: Unscheduled): boolean;

    execBeforeAnyDay(node: AnyDay): boolean;
    execAfterAnyDay(node: AnyDay): boolean;

    execBeforeEventWindow(node: EventWindow): boolean;
    execAfterEventWindow(node: EventWindow): boolean;

    execBeforeComplianceWindow(node: ComplianceWindow): boolean;
    execAfterComplianceWindow(node: ComplianceWindow): boolean;

    execBeforeComplianceWindowOf(node: ComplianceWindowOf): boolean;
    execAfterComplianceWindowOf(node: ComplianceWindowOf): boolean;

    execBeforeStartingBy(node: StartingBy): boolean;
    execAfterStartingBy(node: StartingBy): boolean;

    execBeforeEndingBy(node: EndingBy): boolean;
    execAfterEndingBy(node: EndingBy): boolean;

    execBeforeBetweenTimes(node: BetweenTimes): boolean;
    execAfterBetweenTimes(node: BetweenTimes): boolean;

    execBeforeRepeatCondition(node: RepeatCondition): boolean;
    execAfterRepeatCondition(node: RepeatCondition): boolean;

    execBeforeDaily(node: Daily): boolean;
    execAfterDaily(node: Daily): boolean;

    execBeforeWeekly(node: Weekly): boolean;
    execAfterWeekly(node: Weekly): boolean;

    execBeforeMonthly(node: Monthly): boolean;
    execAfterMonthly(node: Monthly): boolean;

    execBeforeForever(node: Forever): boolean;
    execAfterForever(node: Forever): boolean;

    execBeforeRepeatEvery(node: RepeatEvery): boolean;
    execAfterRepeatEvery(node: RepeatEvery): boolean;

    execBeforeRepeatCount(node: RepeatCount): boolean;
    execAfterRepeatCount(node: RepeatCount): boolean;

    execBeforePeriod(node: Period): boolean;
    execAfterPeriod(node: Period): boolean;

    execBeforeTypeOfEvent(node: TypeOfEvent): boolean;
    execAfterTypeOfEvent(node: TypeOfEvent): boolean;

    execBeforeEvent(node: Event): boolean;
    execAfterEvent(node: Event): boolean;

    execBeforeAbstractTask(node: AbstractTask): boolean;
    execAfterAbstractTask(node: AbstractTask): boolean;

    execBeforeEventTask(node: EventTask): boolean;
    execAfterEventTask(node: EventTask): boolean;

    execBeforeTask(node: Task): boolean;
    execAfterTask(node: Task): boolean;

    execBeforeSharedTask(node: SharedTask): boolean;
    execAfterSharedTask(node: SharedTask): boolean;

    execBeforeTaskReference(node: TaskReference): boolean;
    execAfterTaskReference(node: TaskReference): boolean;

    execBeforeStep(node: Step): boolean;
    execAfterStep(node: Step): boolean;

    execBeforeWorkflowDescription(node: WorkflowDescription): boolean;
    execAfterWorkflowDescription(node: WorkflowDescription): boolean;

    execBeforeWorkflowBranch(node: WorkflowBranch): boolean;
    execAfterWorkflowBranch(node: WorkflowBranch): boolean;

    execBeforeDescription(node: Description): boolean;
    execAfterDescription(node: Description): boolean;

    execBeforeAbstractSystemAccess(node: AbstractSystemAccess): boolean;
    execAfterAbstractSystemAccess(node: AbstractSystemAccess): boolean;

    execBeforeSystemAccess(node: SystemAccess): boolean;
    execAfterSystemAccess(node: SystemAccess): boolean;

    execBeforeSystemAccessReference(node: SystemAccessReference): boolean;
    execAfterSystemAccessReference(node: SystemAccessReference): boolean;

    execBeforeAccessedAtURL(node: AccessedAtURL): boolean;
    execAfterAccessedAtURL(node: AccessedAtURL): boolean;

    execBeforeAccessedAPhoneNumber(node: AccessedAPhoneNumber): boolean;
    execAfterAccessedAPhoneNumber(node: AccessedAPhoneNumber): boolean;

    execBeforeStaffRole(node: StaffRole): boolean;
    execAfterStaffRole(node: StaffRole): boolean;

    execBeforeAbstractPerson(node: AbstractPerson): boolean;
    execAfterAbstractPerson(node: AbstractPerson): boolean;

    execBeforePersonReference(node: PersonReference): boolean;
    execAfterPersonReference(node: PersonReference): boolean;

    execBeforePerson(node: Person): boolean;
    execAfterPerson(node: Person): boolean;

    execBeforeReference(node: Reference): boolean;
    execAfterReference(node: Reference): boolean;

    execBeforeRobotMapping(node: RobotMapping): boolean;
    execAfterRobotMapping(node: RobotMapping): boolean;

    execBeforeSourceToTargetMapping(node: SourceToTargetMapping): boolean;
    execAfterSourceToTargetMapping(node: SourceToTargetMapping): boolean;
}
