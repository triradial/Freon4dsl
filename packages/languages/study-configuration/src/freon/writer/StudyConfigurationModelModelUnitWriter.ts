// Generated by the Freon Language Generator.
// TEMPLATE WriterTemplate.generateUnparser(...)

import { type FreNamedNode, FreNodeReference, type FreWriter, type FreNode, isNullOrUndefined, notNullOrUndefined } from "@freon4dsl/core";
import {
    ScheduleExpression,
    EventReferenceExpression,
    PeriodReferenceExpression,
    Time,
    PreviousVisit,
    StartDay,
    CountExpression,
    LiteralExpression,
    String,
    NumberLiteralExpression,
    BinaryExpression,
    MultiplyExpression,
    PlusExpression,
    MinusExpression,
    DivideExpression,
    AndExpression,
    OrExpression,
    ComparisonExpression,
    LessThenExpression,
    GreaterThenExpression,
    EqualsExpression,
    TimeExpression,
    PatientHistory,
    PatientVisitStatus,
    PatientVisit,
    PatientNotAvailable,
    StaffLevel,
    Month,
    VisitDate,
    DateRange,
    DateConcept,
    EventSchedule,
    EventReference,
    TimeUnit,
    PhaseState,
    EventState,
    SimpleOperators,
    Days,
    TimeAmount,
    TimeAmountPart,
    Day,
    When,
    StudyStart,
    FirstDayOfStudy,
    Baseline,
    Previous,
    Unscheduled,
    AnyDay,
    EventWindow,
    ComplianceWindow,
    ComplianceWindowOf,
    StartingBy,
    EndingBy,
    BetweenTimes,
    RepeatCondition,
    Daily,
    Weekly,
    Monthly,
    Forever,
    RepeatEvery,
    RepeatCount,
    Period,
    TypeOfEvent,
    Event,
    AbstractTask,
    EventTask,
    Task,
    SharedTask,
    TaskReference,
    Step,
    WorkflowDescription,
    WorkflowBranch,
    Description,
    AbstractSystemAccess,
    SystemAccess,
    SystemAccessReference,
    AccessedAtURL,
    AccessedAPhoneNumber,
    StaffRole,
    AbstractPerson,
    PersonReference,
    Person,
    Reference,
    RobotMapping,
    SourceToTargetMapping,
    type EventStart,
    type EventTimeOfDay,
    type RepeatExpression,
    type RepeatUnit,
    type WorkflowDecision,
    type AccessedAt,
    Availability,
    PatientInfo,
    PatientHistoryUnit,
    StudyConfiguration,
} from "../language/index.js";

/**
 * SeparatorType is used to unparse lists.
 * NONE means only space(s) between the elements.
 * Terminator means that every element is terminated with a certain string.
 * Separator means that in between elements a certain string is placed.
 */
enum SeparatorType {
    NONE = "NONE",
    Terminator = "Terminator",
    Separator = "Separator",
    Initiator = "Initiator",
}

/**
 * Class StudyConfigurationModelModelUnitWriter provides methods to return a string representation of an instance of
 * elements of language StudyConfigurationModel.
 * It is, amongst others, used to create error messages in the validator.
 */
export class StudyConfigurationModelModelUnitWriter implements FreWriter {
    output: string[] = []; // stores the result, one line per array element
    currentLine: number = 0; // keeps track of the element in 'output' that we are working on

    /**
     * Returns a string representation of 'node'.
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     * Note that the single-line-string cannot be parsed into a correct model.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToString(node: FreNode, startIndent?: number, short?: boolean): string {
        this.writeToLines(node, startIndent, short);
        return `${this.output
            .map((line) => `${line.trimEnd()}`)
            .join("\n")
            .trimEnd()}`;
    }

    /**
     * Returns a string representation of 'node', divided into an array of strings,
     * each of which contain a single line (without newline).
     * If 'short' is present and true, then a single-line result will be given.
     * Otherwise, the result is always a multi-line string.
     *
     * @param node
     * @param startIndent
     * @param short
     */
    public writeToLines(node: FreNode, startIndent?: number, short?: boolean): string[] {
        // set default for optional parameters
        if (startIndent === undefined) {
            startIndent = 0;
        }
        if (short === undefined) {
            short = false;
        }

        // make sure the global variables are reset
        this.output = [];
        this.currentLine = 0;

        // begin the unparsing with an indent if asked for
        let indentString: string = "";
        for (let _i = 0; _i < startIndent; _i++) {
            indentString += " ";
        }
        this.output[this.currentLine] = indentString;

        // do the actual work
        this.unparse(node, short);
        return this.output;
    }

    /**
     * Returns the name of 'node' if it has one, else returns
     * a short unparsing of 'node'.
     * Used by the validator to produce readable error messages.
     *
     * @param node
     */
    public writeNameOnly(node: FreNode | undefined): string {
        if (isNullOrUndefined(node)) {
            return "";
        }

        if (node instanceof Availability) {
            return node.name;
        } else if (node instanceof PatientInfo) {
            return node.name;
        } else if (node instanceof PatientHistoryUnit) {
            return node.name;
        } else if (node instanceof StudyConfiguration) {
            return node.name;
        } else if (node instanceof PatientVisitStatus) {
            return node.name;
        } else if (node instanceof PatientVisit) {
            return node.name;
        } else if (node instanceof Month) {
            return node.name;
        } else if (node instanceof TimeUnit) {
            return node.name;
        } else if (node instanceof PhaseState) {
            return node.name;
        } else if (node instanceof EventState) {
            return node.name;
        } else if (node instanceof SimpleOperators) {
            return node.name;
        } else if (node instanceof Period) {
            return node.name;
        } else if (node instanceof TypeOfEvent) {
            return node.name;
        } else if (node instanceof Event) {
            return node.name;
        } else if (node instanceof Task) {
            return node.name;
        } else if (node instanceof SharedTask) {
            return node.name;
        } else if (node instanceof Step) {
            return node.name;
        } else if (node instanceof SystemAccess) {
            return node.name;
        } else if (node instanceof StaffRole) {
            return node.name;
        } else if (node instanceof Person) {
            return node.name;
        } else if (node instanceof RobotMapping) {
            return node.name;
        } else {
            // make sure the global variables are reset
            this.output = [];
            this.currentLine = 0;
            // do not care about indent, we just need a single line
            this.output[this.currentLine] = "";
            // do the actual work
            this.unparse(node, true);
            return this.output[0].trimEnd();
        }
    }

    private unparse(node: FreNode, short: boolean) {
        if (isNullOrUndefined(node)) {
            return;
        }
        switch (node.freLanguageConcept()) {
            case "ScheduleExpression":
                this.unparseScheduleExpression(node as ScheduleExpression, short);
                break;
            case "EventReferenceExpression":
                this.unparseEventReferenceExpression(node as EventReferenceExpression, short);
                break;
            case "PeriodReferenceExpression":
                this.unparsePeriodReferenceExpression(node as PeriodReferenceExpression, short);
                break;
            case "Time":
                this.unparseTime(node as Time, short);
                break;
            case "PreviousVisit":
                this.unparsePreviousVisit(node as PreviousVisit, short);
                break;
            case "StartDay":
                this.unparseStartDay(node as StartDay, short);
                break;
            case "CountExpression":
                this.unparseCountExpression(node as CountExpression, short);
                break;
            case "LiteralExpression":
                this.unparseLiteralExpression(node as LiteralExpression, short);
                break;
            case "String":
                this.unparseString(node as String, short);
                break;
            case "NumberLiteralExpression":
                this.unparseNumberLiteralExpression(node as NumberLiteralExpression, short);
                break;
            case "BinaryExpression":
                this.unparseBinaryExpression(node as BinaryExpression, short);
                break;
            case "MultiplyExpression":
                this.unparseMultiplyExpression(node as MultiplyExpression, short);
                break;
            case "PlusExpression":
                this.unparsePlusExpression(node as PlusExpression, short);
                break;
            case "MinusExpression":
                this.unparseMinusExpression(node as MinusExpression, short);
                break;
            case "DivideExpression":
                this.unparseDivideExpression(node as DivideExpression, short);
                break;
            case "AndExpression":
                this.unparseAndExpression(node as AndExpression, short);
                break;
            case "OrExpression":
                this.unparseOrExpression(node as OrExpression, short);
                break;
            case "ComparisonExpression":
                this.unparseComparisonExpression(node as ComparisonExpression, short);
                break;
            case "LessThenExpression":
                this.unparseLessThenExpression(node as LessThenExpression, short);
                break;
            case "GreaterThenExpression":
                this.unparseGreaterThenExpression(node as GreaterThenExpression, short);
                break;
            case "EqualsExpression":
                this.unparseEqualsExpression(node as EqualsExpression, short);
                break;
            case "TimeExpression":
                this.unparseTimeExpression(node as TimeExpression, short);
                break;
            case "PatientHistory":
                this.unparsePatientHistory(node as PatientHistory, short);
                break;
            case "PatientVisitStatus":
                this.unparsePatientVisitStatus(node as PatientVisitStatus, short);
                break;
            case "PatientVisit":
                this.unparsePatientVisit(node as PatientVisit, short);
                break;
            case "PatientNotAvailable":
                this.unparsePatientNotAvailable(node as PatientNotAvailable, short);
                break;
            case "StaffLevel":
                this.unparseStaffLevel(node as StaffLevel, short);
                break;
            case "Month":
                this.unparseMonth(node as Month, short);
                break;
            case "VisitDate":
                this.unparseVisitDate(node as VisitDate, short);
                break;
            case "DateRange":
                this.unparseDateRange(node as DateRange, short);
                break;
            case "DateConcept":
                this.unparseDateConcept(node as DateConcept, short);
                break;
            case "EventSchedule":
                this.unparseEventSchedule(node as EventSchedule, short);
                break;
            case "EventReference":
                this.unparseEventReference(node as EventReference, short);
                break;
            case "TimeUnit":
                this.unparseTimeUnit(node as TimeUnit, short);
                break;
            case "PhaseState":
                this.unparsePhaseState(node as PhaseState, short);
                break;
            case "EventState":
                this.unparseEventState(node as EventState, short);
                break;
            case "SimpleOperators":
                this.unparseSimpleOperators(node as SimpleOperators, short);
                break;
            case "Days":
                this.unparseDays(node as Days, short);
                break;
            case "TimeAmount":
                this.unparseTimeAmount(node as TimeAmount, short);
                break;
            case "TimeAmountPart":
                this.unparseTimeAmountPart(node as TimeAmountPart, short);
                break;
            case "Day":
                this.unparseDay(node as Day, short);
                break;
            case "When":
                this.unparseWhen(node as When, short);
                break;
            case "StudyStart":
                this.unparseStudyStart(node as StudyStart, short);
                break;
            case "FirstDayOfStudy":
                this.unparseFirstDayOfStudy(node as FirstDayOfStudy, short);
                break;
            case "Baseline":
                this.unparseBaseline(node as Baseline, short);
                break;
            case "Previous":
                this.unparsePrevious(node as Previous, short);
                break;
            case "Unscheduled":
                this.unparseUnscheduled(node as Unscheduled, short);
                break;
            case "AnyDay":
                this.unparseAnyDay(node as AnyDay, short);
                break;
            case "EventWindow":
                this.unparseEventWindow(node as EventWindow, short);
                break;
            case "ComplianceWindow":
                this.unparseComplianceWindow(node as ComplianceWindow, short);
                break;
            case "ComplianceWindowOf":
                this.unparseComplianceWindowOf(node as ComplianceWindowOf, short);
                break;
            case "StartingBy":
                this.unparseStartingBy(node as StartingBy, short);
                break;
            case "EndingBy":
                this.unparseEndingBy(node as EndingBy, short);
                break;
            case "BetweenTimes":
                this.unparseBetweenTimes(node as BetweenTimes, short);
                break;
            case "RepeatCondition":
                this.unparseRepeatCondition(node as RepeatCondition, short);
                break;
            case "Daily":
                this.unparseDaily(node as Daily, short);
                break;
            case "Weekly":
                this.unparseWeekly(node as Weekly, short);
                break;
            case "Monthly":
                this.unparseMonthly(node as Monthly, short);
                break;
            case "Forever":
                this.unparseForever(node as Forever, short);
                break;
            case "RepeatEvery":
                this.unparseRepeatEvery(node as RepeatEvery, short);
                break;
            case "RepeatCount":
                this.unparseRepeatCount(node as RepeatCount, short);
                break;
            case "Period":
                this.unparsePeriod(node as Period, short);
                break;
            case "TypeOfEvent":
                this.unparseTypeOfEvent(node as TypeOfEvent, short);
                break;
            case "Event":
                this.unparseEvent(node as Event, short);
                break;
            case "AbstractTask":
                this.unparseAbstractTask(node as AbstractTask, short);
                break;
            case "EventTask":
                this.unparseEventTask(node as EventTask, short);
                break;
            case "Task":
                this.unparseTask(node as Task, short);
                break;
            case "SharedTask":
                this.unparseSharedTask(node as SharedTask, short);
                break;
            case "TaskReference":
                this.unparseTaskReference(node as TaskReference, short);
                break;
            case "Step":
                this.unparseStep(node as Step, short);
                break;
            case "WorkflowDescription":
                this.unparseWorkflowDescription(node as WorkflowDescription, short);
                break;
            case "WorkflowBranch":
                this.unparseWorkflowBranch(node as WorkflowBranch, short);
                break;
            case "Description":
                this.unparseDescription(node as Description, short);
                break;
            case "AbstractSystemAccess":
                this.unparseAbstractSystemAccess(node as AbstractSystemAccess, short);
                break;
            case "SystemAccess":
                this.unparseSystemAccess(node as SystemAccess, short);
                break;
            case "SystemAccessReference":
                this.unparseSystemAccessReference(node as SystemAccessReference, short);
                break;
            case "AccessedAtURL":
                this.unparseAccessedAtURL(node as AccessedAtURL, short);
                break;
            case "AccessedAPhoneNumber":
                this.unparseAccessedAPhoneNumber(node as AccessedAPhoneNumber, short);
                break;
            case "StaffRole":
                this.unparseStaffRole(node as StaffRole, short);
                break;
            case "AbstractPerson":
                this.unparseAbstractPerson(node as AbstractPerson, short);
                break;
            case "PersonReference":
                this.unparsePersonReference(node as PersonReference, short);
                break;
            case "Person":
                this.unparsePerson(node as Person, short);
                break;
            case "Reference":
                this.unparseReference(node as Reference, short);
                break;
            case "RobotMapping":
                this.unparseRobotMapping(node as RobotMapping, short);
                break;
            case "SourceToTargetMapping":
                this.unparseSourceToTargetMapping(node as SourceToTargetMapping, short);
                break;
            case "Availability":
                this.unparseAvailability(node as Availability, short);
                break;
            case "PatientInfo":
                this.unparsePatientInfo(node as PatientInfo, short);
                break;
            case "PatientHistoryUnit":
                this.unparsePatientHistoryUnit(node as PatientHistoryUnit, short);
                break;
            case "StudyConfiguration":
                this.unparseStudyConfiguration(node as StudyConfiguration, short);
                break;
            case "EventStart":
                this.unparseEventStart(node as EventStart, short);
                break;
            case "EventTimeOfDay":
                this.unparseEventTimeOfDay(node as EventTimeOfDay, short);
                break;
            case "RepeatExpression":
                this.unparseRepeatExpression(node as RepeatExpression, short);
                break;
            case "RepeatUnit":
                this.unparseRepeatUnit(node as RepeatUnit, short);
                break;
            case "WorkflowDecision":
                this.unparseWorkflowDecision(node as WorkflowDecision, short);
                break;
            case "AccessedAt":
                this.unparseAccessedAt(node as AccessedAt, short);
                break;
            default:
                console.error("Cannot unparse a " + node.freLanguageConcept());
        }
    }

    /**
     * Unparsing of 'Availability' according to projection 'parser'.
     */
    private unparseAvailability(node: Availability, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `STAFFING `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 2);
            this.output[this.currentLine] += `Number of CRCs Normally Available: `;
            this.output[this.currentLine] += notNullOrUndefined(node.baselineStaff) ? `"${node.baselineStaff}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 2);
            this.output[this.currentLine] += `Staffing Level Changes: `;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(
                node.staffLevels,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += "";
            ("");
        }
    }

    /**
     * Unparsing of 'PatientNotAvailable' according to projection 'parser'.
     */
    private unparsePatientNotAvailable(node: PatientNotAvailable, short: boolean) {
        this._unparseList(node.dates, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
            this.unparse(node, short),
        );
    }

    /**
     * Unparsing of 'StaffLevel' according to projection 'parser'.
     */
    private unparseStaffLevel(node: StaffLevel, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.staffAvailable) ? `"${node.staffAvailable}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `on: `;
        this.unparse(node.dateOrRange, short);
    }

    /**
     * Unparsing of 'DateRange' according to projection 'parser'.
     */
    private unparseDateRange(node: DateRange, short: boolean) {
        this.unparse(node.startDate, short);
        this.output[this.currentLine] += `thru: `;
        if (notNullOrUndefined(node.endDate)) {
            this.unparse(node.endDate, short);
        }
    }

    /**
     * Unparsing of 'DateConcept' according to projection 'parser'.
     */
    private unparseDateConcept(node: DateConcept, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.dateAsString) ? `"${node.dateAsString}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'PatientInfo' according to projection 'parser'.
     */
    private unparsePatientInfo(node: PatientInfo, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Patient Histories: `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(
                node.patientHistories,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'PatientHistory' according to projection 'parser'.
     */
    private unparsePatientHistory(node: PatientHistory, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Patient ID: `;
        this.output[this.currentLine] += notNullOrUndefined(node.patient_id) ? `"${node.patient_id}" ` : `""` /* 5 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 1);
            this.output[this.currentLine] += `Visits: `;
            this.newlineAndIndentation(blockIndent + 3);
            this._unparseList(
                node.patientVisits,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 1);
            this.output[this.currentLine] += `Not Available Dates: `;
            this.newlineAndIndentation(blockIndent + 3);
            this._unparseList(
                node.patientNotAvailableDates,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'PatientVisit' according to projection 'parser'.
     */
    private unparsePatientVisit(node: PatientVisit, short: boolean) {
        this.output[this.currentLine] += `Visit: `;
        this._unparseReference(node.visit, short);
        this.output[this.currentLine] += `was `;
        this._unparseReference(node.status, short);
        this.output[this.currentLine] += `on `;
        this.unparse(node.actualVisitDate, short);
    }

    /**
     * Unparsing of 'PatientHistoryUnit' according to projection 'parser'.
     */
    private unparsePatientHistoryUnit(node: PatientHistoryUnit, short: boolean) {
        this.unparse(node.patientHistory, short);
    }

    /**
     * Unparsing of 'EventSchedule' according to projection 'parser'.
     */
    private unparseEventSchedule(node: EventSchedule, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `First scheduled `;
        this.unparse(node.eventStart, short);

        if (!short) {
            // do the rest of the lines as well
            if (notNullOrUndefined(node.eventWindow)) {
                this.newlineAndIndentation(blockIndent + 0 + -7);
                this.unparse(node.eventWindow, short);
            }
            if (notNullOrUndefined(node.eventRepeat)) {
                this.newlineAndIndentation(blockIndent + 0 + -7);
                this.output[this.currentLine] += `and then repeats `;
                this.unparse(node.eventRepeat, short);
            }
            if (notNullOrUndefined(node.eventTimeOfDay)) {
                this.newlineAndIndentation(blockIndent + 0 + -7);
                this.output[this.currentLine] += `limited to `;
                this.unparse(node.eventTimeOfDay, short);
            }
        }
    }

    /**
     * Unparsing of 'EventReference' according to projection 'parser'.
     */
    private unparseEventReference(node: EventReference, short: boolean) {
        this._unparseReference(node.event, short);
        this._unparseReference(node.eventState, short);
    }

    /**
     * Unparsing of 'Daily' according to projection 'parser'.
     */
    private unparseDaily(node: Daily, short: boolean) {
        this.output[this.currentLine] += `daily `;
    }

    /**
     * Unparsing of 'Weekly' according to projection 'parser'.
     */
    private unparseWeekly(node: Weekly, short: boolean) {
        this.output[this.currentLine] += `weekly `;
    }

    /**
     * Unparsing of 'Monthly' according to projection 'parser'.
     */
    private unparseMonthly(node: Monthly, short: boolean) {
        this.output[this.currentLine] += `monthly `;
    }

    /**
     * Unparsing of 'Forever' according to projection 'parser'.
     */
    private unparseForever(node: Forever, short: boolean) {
        this.output[this.currentLine] += `forever `;
    }

    /**
     * Unparsing of 'TimeAmount' according to projection 'parser'.
     */
    private unparseTimeAmount(node: TimeAmount, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.value) ? `${node.value} ` : `<no-value> ` /* 2 */;
        this._unparseReference(node.unit, short);
    }

    /**
     * Unparsing of 'TimeAmountPart' according to projection 'parser'.
     */
    private unparseTimeAmountPart(node: TimeAmountPart, short: boolean) {
        this._unparseReference(node.operator, short);
        this.unparse(node.timeAmount, short);
    }

    /**
     * Unparsing of 'Days' according to projection 'parser'.
     */
    private unparseDays(node: Days, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.count) ? `${node.count} ` : `<no-value> ` /* 2 */;
    }

    /**
     * Unparsing of 'StudyStart' according to projection 'parser'.
     */
    private unparseStudyStart(node: StudyStart, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.dummyText) ? `"${node.dummyText}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `as the start day of the study `;
    }

    /**
     * Unparsing of 'FirstDayOfStudy' according to projection 'parser'.
     */
    private unparseFirstDayOfStudy(node: FirstDayOfStudy, short: boolean) {
        this.output[this.currentLine] += `on the start day of the study `;
        if (notNullOrUndefined(node.timeAmountPart)) {
            this.unparse(node.timeAmountPart, short);
        }
    }

    /**
     * Unparsing of 'Baseline' according to projection 'parser'.
     */
    private unparseBaseline(node: Baseline, short: boolean) {
        this.output[this.currentLine] += `on the baseline\/start day of the study `;
        if (notNullOrUndefined(node.timeAmountPart)) {
            this.unparse(node.timeAmountPart, short);
        }
    }

    /**
     * Unparsing of 'Day' according to projection 'parser'.
     */
    private unparseDay(node: Day, short: boolean) {
        this.output[this.currentLine] += `day `;
        this.output[this.currentLine] += notNullOrUndefined(node.startDay) ? `${node.startDay} ` : `<no-value> ` /* 2 */;
    }

    /**
     * Unparsing of 'When' according to projection 'parser'.
     */
    private unparseWhen(node: When, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        this.output[this.currentLine] += `when `;
        this.unparse(node.startWhen, short);
        if (notNullOrUndefined(node.timeAmountPart)) {
            this.unparse(node.timeAmountPart, short);
        }
    }

    /**
     * Unparsing of 'Previous' according to projection 'parser'.
     */
    private unparsePrevious(node: Previous, short: boolean) {
        this.output[this.currentLine] += `when the previous event `;
        this._unparseReference(node.eventState, short);
        if (notNullOrUndefined(node.timeAmountPart)) {
            this.unparse(node.timeAmountPart, short);
        }
    }

    /**
     * Unparsing of 'Unscheduled' according to projection 'parser'.
     */
    private unparseUnscheduled(node: Unscheduled, short: boolean) {
        this.output[this.currentLine] += `on any day (an unscheduled event) `;
    }

    /**
     * Unparsing of 'AnyDay' according to projection 'parser'.
     */
    private unparseAnyDay(node: AnyDay, short: boolean) {
        this.output[this.currentLine] += `on any day (an unscheduled event) `;
    }

    /**
     * Unparsing of 'EventWindow' according to projection 'parser'.
     */
    private unparseEventWindow(node: EventWindow, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `with a window of `;
        this.unparse(node.daysBefore, short);
        this.output[this.currentLine] += `day(s) before and `;
        this.unparse(node.daysAfter, short);
        this.output[this.currentLine] += `day(s) after `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.complianceWindow)) {
                this.unparse(node.complianceWindow, short);
            }
        }
    }

    /**
     * Unparsing of 'ComplianceWindowOf' according to projection 'parser'.
     */
    private unparseComplianceWindowOf(node: ComplianceWindowOf, short: boolean) {
        this.output[this.currentLine] += `and a compliance window of `;
        this.unparse(node.maxDaysBefore, short);
        this.output[this.currentLine] += `additional day(s) before and `;
        this.unparse(node.maxDaysAfter, short);
        this.output[this.currentLine] += `day(s) after `;
    }

    /**
     * Unparsing of 'BetweenTimes' according to projection 'parser'.
     */
    private unparseBetweenTimes(node: BetweenTimes, short: boolean) {
        this.output[this.currentLine] += `starting by `;
        this.output[this.currentLine] += notNullOrUndefined(node.startTimeAsString) ? `"${node.startTimeAsString}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `and ending by `;
        this.output[this.currentLine] += notNullOrUndefined(node.endTimeAsString) ? `"${node.endTimeAsString}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'StartingBy' according to projection 'parser'.
     */
    private unparseStartingBy(node: StartingBy, short: boolean) {
        this.output[this.currentLine] += `starting by `;
        this.output[this.currentLine] += notNullOrUndefined(node.startTimeAsString) ? `"${node.startTimeAsString}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'EndingBy' according to projection 'parser'.
     */
    private unparseEndingBy(node: EndingBy, short: boolean) {
        this.output[this.currentLine] += `ending by `;
        this.output[this.currentLine] += notNullOrUndefined(node.endTimeAsString) ? `"${node.endTimeAsString}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'RepeatEvery' according to projection 'parser'.
     */
    private unparseRepeatEvery(node: RepeatEvery, short: boolean) {
        this.output[this.currentLine] += `every: `;
        this.unparse(node.repeatEvery, short);
    }

    /**
     * Unparsing of 'RepeatCondition' according to projection 'parser'.
     */
    private unparseRepeatCondition(node: RepeatCondition, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        this.unparse(node.repeatUnit, short);
        if (notNullOrUndefined(node.maxRepeats)) {
            this.output[this.currentLine] += `up to `;
            this.output[this.currentLine] += notNullOrUndefined(node.maxRepeats) ? `${node.maxRepeats} ` : `<no-value> ` /* 2 */;
            this.output[this.currentLine] += `times `;
        }
        if (notNullOrUndefined(node.until)) {
            this.output[this.currentLine] += `until `;
            this.unparse(node.until, short);
        }
    }

    /**
     * Unparsing of 'RepeatCount' according to projection 'parser'.
     */
    private unparseRepeatCount(node: RepeatCount, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.repeatCount) ? `${node.repeatCount} ` : `<no-value> ` /* 2 */;
        this.output[this.currentLine] += `time(s) `;
    }

    /**
     * Unparsing of 'StudyConfiguration' according to projection 'parser'.
     */
    private unparseStudyConfiguration(node: StudyConfiguration, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Periods: `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.periods, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Tasks: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.tasks, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `System Accesses: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(
                node.systemAccesses,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Staffing: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(
                node.staffing,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Roles: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.roles, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Period' according to projection 'parser'.
     */
    private unparsePeriod(node: Period, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Period: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.unparse(node.description, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Events: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.events, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Event' according to projection 'parser'.
     */
    private unparseEvent(node: Event, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Event: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `This is a `;
            this._unparseReference(node.typeOfEvent, short);
            if (notNullOrUndefined(node.alternativeName)) {
                this.output[this.currentLine] += `that is also referred to as `;
                this.output[this.currentLine] += notNullOrUndefined(node.alternativeName) ? `"${node.alternativeName}" ` : `""` /* 5 */;
            }
            this.newlineAndIndentation(blockIndent + 0);
            this.unparse(node.description, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Schedule: `;
            this.newlineAndIndentation(blockIndent + 2);
            this.unparse(node.schedule, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Tasks: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.tasks, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'SharedTask' according to projection 'parser'.
     */
    private unparseSharedTask(node: SharedTask, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Shared Task: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Steps: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.steps, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'Task' according to projection 'parser'.
     */
    private unparseTask(node: Task, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Task: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Steps: `;
            this.newlineAndIndentation(blockIndent + 2);
            this._unparseList(node.steps, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'TaskReference' according to projection 'parser'.
     */
    private unparseTaskReference(node: TaskReference, short: boolean) {
        this.output[this.currentLine] += `TaskReference: `;
        this._unparseReference(node.task, short);
    }

    /**
     * Unparsing of 'Step' according to projection 'parser'.
     */
    private unparseStep(node: Step, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Step: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `People: `;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(node.people, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Systems Used: `;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(node.systems, "", SeparatorType.Separator, true, this.output[this.currentLine].length, short, (node, short) =>
                this.unparse(node, short),
            );
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `References: `;
            this.newlineAndIndentation(blockIndent + 4);
            this._unparseList(
                node.references,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'SystemAccess' according to projection 'parser'.
     */
    private unparseSystemAccess(node: SystemAccess, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `System Name: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Accessed At: `;
            this.unparse(node.accessedAt, short);
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
        }
    }

    /**
     * Unparsing of 'SystemAccessReference' according to projection 'parser'.
     */
    private unparseSystemAccessReference(node: SystemAccessReference, short: boolean) {
        this.output[this.currentLine] += `SystemAccessReference: `;
        this._unparseReference(node.system, short);
    }

    /**
     * Unparsing of 'AccessedAtURL' according to projection 'parser'.
     */
    private unparseAccessedAtURL(node: AccessedAtURL, short: boolean) {
        this.output[this.currentLine] += `URL: `;
        this.output[this.currentLine] += notNullOrUndefined(node.url) ? `"${node.url}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'AccessedAPhoneNumber' according to projection 'parser'.
     */
    private unparseAccessedAPhoneNumber(node: AccessedAPhoneNumber, short: boolean) {
        this.output[this.currentLine] += `Phone Number: `;
        this.output[this.currentLine] += notNullOrUndefined(node.phoneNumber) ? `"${node.phoneNumber}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'Person' according to projection 'parser'.
     */
    private unparsePerson(node: Person, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Name: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Email: `;
            this.output[this.currentLine] += notNullOrUndefined(node.email) ? `"${node.email}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Phone: `;
            this.output[this.currentLine] += notNullOrUndefined(node.phoneNumber) ? `"${node.phoneNumber}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Role: `;
            this._unparseReference(node.role, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Notes: `;
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
        }
    }

    /**
     * Unparsing of 'PersonReference' according to projection 'parser'.
     */
    private unparsePersonReference(node: PersonReference, short: boolean) {
        this.output[this.currentLine] += `PersonReference: `;
        this._unparseReference(node.person, short);
    }

    /**
     * Unparsing of 'Reference' according to projection 'parser'.
     */
    private unparseReference(node: Reference, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Reference: `;
        this.output[this.currentLine] += notNullOrUndefined(node.name) ? `"${node.name}" ` : `""` /* 5 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `Document is at: `;
            this.output[this.currentLine] += notNullOrUndefined(node.link) ? `"${node.link}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
        }
    }

    /**
     * Unparsing of 'StaffRole' according to projection 'parser'.
     */
    private unparseStaffRole(node: StaffRole, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `Role: `;
        this.output[this.currentLine] += `\`${node.name}\` ` /* 3 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            if (notNullOrUndefined(node.description)) {
                this.unparse(node.description, short);
            }
        }
    }

    /**
     * Unparsing of 'WorkflowDescription' according to projection 'parser'.
     */
    private unparseWorkflowDescription(node: WorkflowDescription, short: boolean) {
        this.output[this.currentLine] += `Description: `;
        this.unparse(node.text, short);
    }

    /**
     * Unparsing of 'WorkflowBranch' according to projection 'parser'.
     */
    private unparseWorkflowBranch(node: WorkflowBranch, short: boolean) {
        this.output[this.currentLine] += `Branch: `;
        this.unparse(node.branch, short);
    }

    /**
     * Unparsing of 'RobotMapping' according to projection 'parser'.
     */
    private unparseRobotMapping(node: RobotMapping, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `From this Page: `;
        this.output[this.currentLine] += notNullOrUndefined(node.sourcePage) ? `"${node.sourcePage}" ` : `""` /* 5 */;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `To this Page    : `;
            this.output[this.currentLine] += notNullOrUndefined(node.targetPage) ? `"${node.targetPage}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 0);
            this._unparseList(
                node.sourceToTargetMappings,
                "",
                SeparatorType.Separator,
                true,
                this.output[this.currentLine].length,
                short,
                (node, short) => this.unparse(node, short),
            );
        }
    }

    /**
     * Unparsing of 'SourceToTargetMapping' according to projection 'parser'.
     */
    private unparseSourceToTargetMapping(node: SourceToTargetMapping, short: boolean) {
        this.output[this.currentLine] += `Source Field: `;
        this.output[this.currentLine] += notNullOrUndefined(node.sourceField) ? `"${node.sourceField}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `Target Field: `;
        this.output[this.currentLine] += notNullOrUndefined(node.targetField) ? `"${node.targetField}" ` : `""` /* 5 */;
    }

    /**
     * Unparsing of 'Description' according to projection 'parser'.
     */
    private unparseDescription(node: Description, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        if (notNullOrUndefined(node.rawText)) {
            this.newlineAndIndentation(blockIndent + 0 + 0);
            this.output[this.currentLine] += `Description: `;
            this.output[this.currentLine] += notNullOrUndefined(node.rawText) ? `"${node.rawText}" ` : `""` /* 5 */;
        }
    }

    /**
     * Unparsing of 'Time' according to projection 'default'.
     */
    private unparseTime(node: Time, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.value) ? `"${node.value}" ` : `""` /* 5 */;
        this._unparseReference(node.unit, short);
    }

    /**
     * Unparsing of 'StartDay' according to projection 'default'.
     */
    private unparseStartDay(node: StartDay, short: boolean) {
        this.output[this.currentLine] += `start day `;
    }

    /**
     * Unparsing of 'EventReferenceExpression' according to projection 'default'.
     */
    private unparseEventReferenceExpression(node: EventReferenceExpression, short: boolean) {
        this._unparseReference(node.event, short);
        this._unparseReference(node.eventState, short);
    }

    /**
     * Unparsing of 'PeriodReferenceExpression' according to projection 'default'.
     */
    private unparsePeriodReferenceExpression(node: PeriodReferenceExpression, short: boolean) {
        this._unparseReference(node.period, short);
        this._unparseReference(node.periodState, short);
    }

    /**
     * Unparsing of 'CountExpression' according to projection 'default'.
     */
    private unparseCountExpression(node: CountExpression, short: boolean) {
        this.output[this.currentLine] += `count( `;
        this._unparseReference(node.event, short);
        this.output[this.currentLine] += `) `;
    }

    /**
     * Unparsing of 'PreviousVisit' according to projection 'default'.
     */
    private unparsePreviousVisit(node: PreviousVisit, short: boolean) {
        this.output[this.currentLine] += `previous visit `;
    }

    /**
     * Unparsing of 'NumberLiteralExpression' according to projection 'default'.
     */
    private unparseNumberLiteralExpression(node: NumberLiteralExpression, short: boolean) {
        this.output[this.currentLine] += notNullOrUndefined(node.value) ? `${node.value} ` : `<no-value> ` /* 2 */;
    }

    /**
     * Unparsing of 'String' according to projection 'default'.
     */
    private unparseString(node: String, short: boolean) {
        this.output[this.currentLine] += `\' `;
        this.output[this.currentLine] += notNullOrUndefined(node.value) ? `"${node.value}" ` : `""` /* 5 */;
        this.output[this.currentLine] += `\' `;
    }

    /**
     * Unparsing of 'VisitDate' according to projection 'default'.
     */
    private unparseVisitDate(node: VisitDate, short: boolean) {
        const blockIndent = this.output[this.currentLine].length;
        // do the first line
        this.output[this.currentLine] += `VisitDate `;
        this.output[this.currentLine] += `\{ `;

        if (!short) {
            // do the rest of the lines as well
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `dateAsString `;
            this.output[this.currentLine] += notNullOrUndefined(node.dateAsString) ? `"${node.dateAsString}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `day `;
            this.output[this.currentLine] += notNullOrUndefined(node.day) ? `"${node.day}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `year `;
            this.output[this.currentLine] += notNullOrUndefined(node.year) ? `"${node.year}" ` : `""` /* 5 */;
            this.newlineAndIndentation(blockIndent + 4);
            this.output[this.currentLine] += `month `;
            this._unparseReference(node.month, short);
            this.newlineAndIndentation(blockIndent + 0);
            this.output[this.currentLine] += `} `;
        }
    }
    /**
     * See the public unparse method.
     */
    private unparsePlusExpression(node: PlusExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "+ ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseMinusExpression(node: MinusExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "- ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseMultiplyExpression(node: MultiplyExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "* ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseDivideExpression(node: DivideExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "/ ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseLessThenExpression(node: LessThenExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "< ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseOrExpression(node: OrExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "or ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseAndExpression(node: AndExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "and ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseGreaterThenExpression(node: GreaterThenExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "> ";
        this.unparse(node.right, short);
    }
    /**
     * See the public unparse method.
     */
    private unparseEqualsExpression(node: EqualsExpression, short: boolean) {
        this.unparse(node.left, short);
        this.output[this.currentLine] += "== ";
        this.unparse(node.right, short);
    }
    /**
     * The limited concept 'PatientVisitStatus' is unparsed as its name.
     */
    private unparsePatientVisitStatus(node: PatientVisitStatus, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'Month' is unparsed as its name.
     */
    private unparseMonth(node: Month, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'TimeUnit' is unparsed as its name.
     */
    private unparseTimeUnit(node: TimeUnit, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'PhaseState' is unparsed as its name.
     */
    private unparsePhaseState(node: PhaseState, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'EventState' is unparsed as its name.
     */
    private unparseEventState(node: EventState, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'SimpleOperators' is unparsed as its name.
     */
    private unparseSimpleOperators(node: SimpleOperators, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The limited concept 'TypeOfEvent' is unparsed as its name.
     */
    private unparseTypeOfEvent(node: TypeOfEvent, short: boolean) {
        if (notNullOrUndefined(node)) {
            this.output[this.currentLine] += "`" + node.name + "` " /* 9 */;
        }
    }
    /**
     * The abstract concept 'ScheduleExpression' is not unparsed.
     */
    private unparseScheduleExpression(node: ScheduleExpression, short: boolean) {
        throw new Error("Method unparseScheduleExpression should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'LiteralExpression' is not unparsed.
     */
    private unparseLiteralExpression(node: LiteralExpression, short: boolean) {
        throw new Error("Method unparseLiteralExpression should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'BinaryExpression' is not unparsed.
     */
    private unparseBinaryExpression(node: BinaryExpression, short: boolean) {
        throw new Error("Method unparseBinaryExpression should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'ComparisonExpression' is not unparsed.
     */
    private unparseComparisonExpression(node: ComparisonExpression, short: boolean) {
        throw new Error("Method unparseComparisonExpression should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'TimeExpression' is not unparsed.
     */
    private unparseTimeExpression(node: TimeExpression, short: boolean) {
        throw new Error("Method unparseTimeExpression should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'ComplianceWindow' is not unparsed.
     */
    private unparseComplianceWindow(node: ComplianceWindow, short: boolean) {
        throw new Error("Method unparseComplianceWindow should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'AbstractTask' is not unparsed.
     */
    private unparseAbstractTask(node: AbstractTask, short: boolean) {
        throw new Error("Method unparseAbstractTask should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'EventTask' is not unparsed.
     */
    private unparseEventTask(node: EventTask, short: boolean) {
        throw new Error("Method unparseEventTask should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'AbstractSystemAccess' is not unparsed.
     */
    private unparseAbstractSystemAccess(node: AbstractSystemAccess, short: boolean) {
        throw new Error("Method unparseAbstractSystemAccess should be implemented by its (concrete) subclasses.");
    }
    /**
     * The abstract concept 'AbstractPerson' is not unparsed.
     */
    private unparseAbstractPerson(node: AbstractPerson, short: boolean) {
        throw new Error("Method unparseAbstractPerson should be implemented by its (concrete) subclasses.");
    }
    /**
     * The interface 'EventStart' is not unparsed.
     */
    private unparseEventStart(node: FreNode, short: boolean) {
        throw new Error("Method unparseEventStart should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'EventTimeOfDay' is not unparsed.
     */
    private unparseEventTimeOfDay(node: FreNode, short: boolean) {
        throw new Error("Method unparseEventTimeOfDay should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'RepeatExpression' is not unparsed.
     */
    private unparseRepeatExpression(node: FreNode, short: boolean) {
        throw new Error("Method unparseRepeatExpression should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'RepeatUnit' is not unparsed.
     */
    private unparseRepeatUnit(node: FreNode, short: boolean) {
        throw new Error("Method unparseRepeatUnit should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'WorkflowDecision' is not unparsed.
     */
    private unparseWorkflowDecision(node: FreNode, short: boolean) {
        throw new Error("Method unparseWorkflowDecision should be implemented by the classes that implement it.");
    }
    /**
     * The interface 'AccessedAt' is not unparsed.
     */
    private unparseAccessedAt(node: FreNode, short: boolean) {
        throw new Error("Method unparseAccessedAt should be implemented by the classes that implement it.");
    }

    /**
     *
     */
    private _unparseReference(node: FreNodeReference<FreNamedNode>, short: boolean) {
        if (notNullOrUndefined(node)) {
            const type: FreNamedNode = node?.referred;
            if (notNullOrUndefined(type)) {
                if (type instanceof PatientVisitStatus) {
                    this.unparsePatientVisitStatus(type, short);
                } else if (type instanceof Month) {
                    this.unparseMonth(type, short);
                } else if (type instanceof TimeUnit) {
                    this.unparseTimeUnit(type, short);
                } else if (type instanceof PhaseState) {
                    this.unparsePhaseState(type, short);
                } else if (type instanceof EventState) {
                    this.unparseEventState(type, short);
                } else if (type instanceof SimpleOperators) {
                    this.unparseSimpleOperators(type, short);
                } else if (type instanceof TypeOfEvent) {
                    this.unparseTypeOfEvent(type, short);
                } else {
                    this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
                }
            } else {
                this.output[this.currentLine] += node.pathname.map((s) => "`" + s + "`").join(".") + " ";
            }
        }
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     * @param method
     * @private
     */
    private _unparseList(
        list: FreNode[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
        method: (node: FreNode, short: boolean) => void,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            method(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of a list of references, where every reference
     * is replaced by the name of its referred element. The use of params
     * 'sepText' and 'SepType' are equals to those in the private method _unparseList.
     * @param list
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseReferenceList(
        list: FreNodeReference<FreNamedNode>[],
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        list.forEach((listElem, index) => {
            const isLastInList: boolean = index === list.length - 1;
            this.doInitiator(sepText, sepType);
            this._unparseReference(listElem, short);
            this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
        });
    }

    /**
     * Adds a string representation of 'list' to the 'output', using 'sepText' , and 'sepType' to include either a separator string
     * or a terminator string. Param 'vertical' indicates whether the list should be represented vertically or horizontally.
     * If 'short' is false, then a multi-line result will be given. Otherwise, only one single-line string is added.
     * @param list
     * @param isIdentifier indicates whether the value should be surrounded with double quotes
     * @param sepText
     * @param sepType
     * @param vertical
     * @param indent
     * @param short
     */
    private _unparseListOfPrimitiveValues(
        list: (string | number | boolean)[],
        isIdentifier: boolean,
        sepText: string,
        sepType: SeparatorType,
        vertical: boolean,
        indent: number,
        short: boolean,
    ) {
        if (notNullOrUndefined(list)) {
            list.forEach((listElem, index) => {
                const isLastInList: boolean = index === list.length - 1;
                this.doInitiator(sepText, sepType);
                if (typeof listElem === "string" && !isIdentifier) {
                    this.output[this.currentLine] += `"${listElem}"`;
                } else if (typeof listElem === "string" && isIdentifier) {
                    this.output[this.currentLine] += `\`${listElem}\``;
                } else {
                    this.output[this.currentLine] += `${listElem}`;
                }
                this.doSeparatorOrTerminatorAndNewline(sepType, isLastInList, sepText, vertical, short, indent);
            });
        }
    }

    /**
     * Adds a separator, terminator, or initiator text (followed or preceded by a newline and the right amount of indentation)
     * to the output, depending on the parameters.
     * @param sepType
     * @param isLastInList
     * @param sepText
     * @param vertical
     * @param short
     * @param indent
     */
    private doSeparatorOrTerminatorAndNewline(
        sepType: SeparatorType,
        isLastInList: boolean,
        sepText: string,
        vertical: boolean,
        short: boolean,
        indent: number,
    ) {
        // first eliminate any whitespace at the end of the line
        this.output[this.currentLine] = this.output[this.currentLine].trimEnd();

        if (!vertical && (!sepText || sepText.length == 0)) {
            // at least separate the items by a space to avoid things
            // like "IntegerFunction", which should be "Integer Function"
            sepText = " ";
        }

        // then add the right separator or terminator
        switch (sepType) {
            case SeparatorType.Separator: {
                if (!isLastInList) {
                    this.output[this.currentLine] += sepText;
                }
                break;
            }
            case SeparatorType.Terminator: {
                this.output[this.currentLine] += sepText;
                break;
            }
            case SeparatorType.Initiator: {
                break;
            }
            case SeparatorType.NONE: {
                if (!vertical) {
                    // at least separate the items by a space to avoid things
                    // like "IntegerFunction", which should be "Integer Function"
                    this.output[this.currentLine] += " ";
                }
                break;
            }
        }

        // then add newline and indentation
        if (vertical && !isLastInList) {
            if (!short) {
                this.newlineAndIndentation(indent);
            } else {
                // stop after 1 line
                // note that the following cannot be parsed
                this.output[this.currentLine] += ` ...`;
            }
        } else if (isLastInList) {
            // end with a space to avoid things
            // like "666after", which should be "666 after"
            if (this.output[this.currentLine][this.output[this.currentLine].length - 1] !== " ") {
                this.output[this.currentLine] += ` `;
            }
        }
    }

    /**
     * Makes a new entry in the 'output' array
     * and adds the indentation of 'number' spaces
     * to the new entry/line.
     * @param indent
     */
    private newlineAndIndentation(indent: number) {
        this.currentLine += 1;
        let indentation: string = "";
        for (let _i = 0; _i < indent; _i++) {
            indentation += " ";
        }
        this.output[this.currentLine] = indentation;
    }

    /**
     * Adds the 'initiator' text
     * @param sepText
     * @param sepType
     * @private
     */
    private doInitiator(sepText: string, sepType: SeparatorType) {
        if (sepType === SeparatorType.Initiator) {
            const nrOfWhiteSpaces = this.output[this.currentLine].split("").filter((char) => /\s/.test(char)).length;
            const onlyIndentation = this.output[this.currentLine].length === nrOfWhiteSpaces;
            if (onlyIndentation) {
                this.output[this.currentLine] += sepText;
            } else {
                // add space before initiator, if it is not the first on the line
                this.output[this.currentLine] += " " + sepText;
            }
        }
    }
}
