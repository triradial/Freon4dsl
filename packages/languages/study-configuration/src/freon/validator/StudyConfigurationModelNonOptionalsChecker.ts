// Generated by the Freon Language Generator.
// TEMPLATE: NonOptionalsCheckerTemplate.generateChecker(...)

import { FreError, FreErrorSeverity, type FreWriter, FreLanguageEnvironment, isNullOrUndefined } from "@freon4dsl/core";
import {
    StudyConfigurationModel,
    Availability,
    PatientInfo,
    PatientHistoryUnit,
    StudyConfiguration,
    EventReferenceExpression,
    PeriodReferenceExpression,
    Time,
    CountExpression,
    String,
    NumberLiteralExpression,
    BinaryExpression,
    MultiplyExpression,
    PlusExpression,
    MinusExpression,
    DivideExpression,
    AndExpression,
    OrExpression,
    ComparisonExpression,
    LessThenExpression,
    GreaterThenExpression,
    EqualsExpression,
    PatientHistory,
    PatientVisitStatus,
    PatientVisit,
    StaffLevel,
    Month,
    VisitDate,
    DateRange,
    DateConcept,
    EventSchedule,
    EventReference,
    TimeUnit,
    PhaseState,
    EventState,
    SimpleOperators,
    Days,
    TimeAmount,
    TimeAmountPart,
    Day,
    When,
    Previous,
    EventWindow,
    ComplianceWindowOf,
    StartingBy,
    EndingBy,
    BetweenTimes,
    RepeatCondition,
    RepeatEvery,
    RepeatCount,
    Period,
    TypeOfEvent,
    Event,
    Task,
    SharedTask,
    TaskReference,
    Step,
    WorkflowDescription,
    WorkflowBranch,
    SystemAccess,
    SystemAccessReference,
    StaffRole,
    PersonReference,
    Person,
    Reference,
    RobotMapping,
    SourceToTargetMapping,
} from "../language/index.js";

import { StudyConfigurationModelDefaultWorker } from "../utils/index.js";
import { type StudyConfigurationModelCheckerInterface } from "./StudyConfigurationModelValidator.js";
import { locationDescription } from "../../custom/CustomLocationDescription.js";

/**
 * Class StudyConfigurationModelNonOptionalsChecker is part of the implementation of the default validator.
 * It checks whether non-optional properties, as such defined in the .ast definition, indeed
 * have a value.
 * Class StudyConfigurationModelWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class StudyConfigurationModelNonOptionalsChecker
    extends StudyConfigurationModelDefaultWorker
    implements StudyConfigurationModelCheckerInterface
{
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStudyConfigurationModel(node: StudyConfigurationModel): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.configuration)) {
            this.errorList.push(
                new FreError(
                    `Property 'configuration' of ${locationDescription(node)} must have a value`,
                    node,
                    "configuration",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.availability)) {
            this.errorList.push(
                new FreError(
                    `Property 'availability' of ${locationDescription(node)} must have a value`,
                    node,
                    "availability",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.patients)) {
            this.errorList.push(
                new FreError(
                    `Property 'patients' of ${locationDescription(node)} must have a value`,
                    node,
                    "patients",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.currentPatient)) {
            this.errorList.push(
                new FreError(
                    `Property 'currentPatient' of ${locationDescription(node)} must have a value`,
                    node,
                    "currentPatient",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeAvailability(node: Availability): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.baselineStaff) || node.baselineStaff?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'baselineStaff' of ${locationDescription(node)} must have a value`,
                    node,
                    "baselineStaff",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientInfo(node: PatientInfo): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientHistoryUnit(node: PatientHistoryUnit): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.patientHistory)) {
            this.errorList.push(
                new FreError(
                    `Property 'patientHistory' of ${locationDescription(node)} must have a value`,
                    node,
                    "patientHistory",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStudyConfiguration(node: StudyConfiguration): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.showSharedTasks)) {
            this.errorList.push(
                new FreError(
                    `Property 'showSharedTasks' of ${locationDescription(node)} must have a value`,
                    node,
                    "showSharedTasks",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showScheduling)) {
            this.errorList.push(
                new FreError(
                    `Property 'showScheduling' of ${locationDescription(node)} must have a value`,
                    node,
                    "showScheduling",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showSchedulingDetails)) {
            this.errorList.push(
                new FreError(
                    `Property 'showSchedulingDetails' of ${locationDescription(node)} must have a value`,
                    node,
                    "showSchedulingDetails",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showChecklists)) {
            this.errorList.push(
                new FreError(
                    `Property 'showChecklists' of ${locationDescription(node)} must have a value`,
                    node,
                    "showChecklists",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showSteps)) {
            this.errorList.push(
                new FreError(
                    `Property 'showSteps' of ${locationDescription(node)} must have a value`,
                    node,
                    "showSteps",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showReferences)) {
            this.errorList.push(
                new FreError(
                    `Property 'showReferences' of ${locationDescription(node)} must have a value`,
                    node,
                    "showReferences",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showSystems)) {
            this.errorList.push(
                new FreError(
                    `Property 'showSystems' of ${locationDescription(node)} must have a value`,
                    node,
                    "showSystems",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showPeople)) {
            this.errorList.push(
                new FreError(
                    `Property 'showPeople' of ${locationDescription(node)} must have a value`,
                    node,
                    "showPeople",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showDescriptions)) {
            this.errorList.push(
                new FreError(
                    `Property 'showDescriptions' of ${locationDescription(node)} must have a value`,
                    node,
                    "showDescriptions",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.studyStartDayNumber)) {
            this.errorList.push(
                new FreError(
                    `Property 'studyStartDayNumber' of ${locationDescription(node)} must have a value`,
                    node,
                    "studyStartDayNumber",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventReferenceExpression(node: EventReferenceExpression): boolean {
        if (isNullOrUndefined(node.event)) {
            this.errorList.push(
                new FreError(`Property 'event' of ${locationDescription(node)} must have a value`, node, "event", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.eventState)) {
            this.errorList.push(
                new FreError(
                    `Property 'eventState' of ${locationDescription(node)} must have a value`,
                    node,
                    "eventState",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePeriodReferenceExpression(node: PeriodReferenceExpression): boolean {
        if (isNullOrUndefined(node.period)) {
            this.errorList.push(
                new FreError(`Property 'period' of ${locationDescription(node)} must have a value`, node, "period", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.periodState)) {
            this.errorList.push(
                new FreError(
                    `Property 'periodState' of ${locationDescription(node)} must have a value`,
                    node,
                    "periodState",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTime(node: Time): boolean {
        if (isNullOrUndefined(node.value) || node.value?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'value' of ${locationDescription(node)} must have a value`, node, "value", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.unit)) {
            this.errorList.push(
                new FreError(`Property 'unit' of ${locationDescription(node)} must have a value`, node, "unit", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeCountExpression(node: CountExpression): boolean {
        if (isNullOrUndefined(node.event)) {
            this.errorList.push(
                new FreError(`Property 'event' of ${locationDescription(node)} must have a value`, node, "event", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeString(node: String): boolean {
        if (isNullOrUndefined(node.value) || node.value?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'value' of ${locationDescription(node)} must have a value`, node, "value", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeNumberLiteralExpression(node: NumberLiteralExpression): boolean {
        if (isNullOrUndefined(node.value)) {
            this.errorList.push(
                new FreError(`Property 'value' of ${locationDescription(node)} must have a value`, node, "value", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeBinaryExpression(node: BinaryExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMultiplyExpression(node: MultiplyExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePlusExpression(node: PlusExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMinusExpression(node: MinusExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDivideExpression(node: DivideExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeAndExpression(node: AndExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeOrExpression(node: OrExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeComparisonExpression(node: ComparisonExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeLessThenExpression(node: LessThenExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeGreaterThenExpression(node: GreaterThenExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEqualsExpression(node: EqualsExpression): boolean {
        if (isNullOrUndefined(node.left)) {
            this.errorList.push(
                new FreError(`Property 'left' of ${locationDescription(node)} must have a value`, node, "left", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.right)) {
            this.errorList.push(
                new FreError(`Property 'right' of ${locationDescription(node)} must have a value`, node, "right", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientHistory(node: PatientHistory): boolean {
        if (isNullOrUndefined(node.patient_id) || node.patient_id?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'patient_id' of ${locationDescription(node)} must have a value`,
                    node,
                    "patient_id",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.id) || node.id?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'id' of ${locationDescription(node)} must have a value`, node, "id", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.startOfStudyDate)) {
            this.errorList.push(
                new FreError(
                    `Property 'startOfStudyDate' of ${locationDescription(node)} must have a value`,
                    node,
                    "startOfStudyDate",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientVisitStatus(node: PatientVisitStatus): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientVisit(node: PatientVisit): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.visitInstanceNumber)) {
            this.errorList.push(
                new FreError(
                    `Property 'visitInstanceNumber' of ${locationDescription(node)} must have a value`,
                    node,
                    "visitInstanceNumber",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.actualVisitDate)) {
            this.errorList.push(
                new FreError(
                    `Property 'actualVisitDate' of ${locationDescription(node)} must have a value`,
                    node,
                    "actualVisitDate",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.visit)) {
            this.errorList.push(
                new FreError(`Property 'visit' of ${locationDescription(node)} must have a value`, node, "visit", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.status)) {
            this.errorList.push(
                new FreError(`Property 'status' of ${locationDescription(node)} must have a value`, node, "status", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStaffLevel(node: StaffLevel): boolean {
        if (isNullOrUndefined(node.staffAvailable) || node.staffAvailable?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'staffAvailable' of ${locationDescription(node)} must have a value`,
                    node,
                    "staffAvailable",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.startDate)) {
            this.errorList.push(
                new FreError(
                    `Property 'startDate' of ${locationDescription(node)} must have a value`,
                    node,
                    "startDate",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.dateOrRange)) {
            this.errorList.push(
                new FreError(
                    `Property 'dateOrRange' of ${locationDescription(node)} must have a value`,
                    node,
                    "dateOrRange",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMonth(node: Month): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeVisitDate(node: VisitDate): boolean {
        if (isNullOrUndefined(node.dateAsString) || node.dateAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'dateAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "dateAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.day) || node.day?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'day' of ${locationDescription(node)} must have a value`, node, "day", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.year) || node.year?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'year' of ${locationDescription(node)} must have a value`, node, "year", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.month)) {
            this.errorList.push(
                new FreError(`Property 'month' of ${locationDescription(node)} must have a value`, node, "month", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDateRange(node: DateRange): boolean {
        if (isNullOrUndefined(node.startDate)) {
            this.errorList.push(
                new FreError(
                    `Property 'startDate' of ${locationDescription(node)} must have a value`,
                    node,
                    "startDate",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDateConcept(node: DateConcept): boolean {
        if (isNullOrUndefined(node.dateAsString) || node.dateAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'dateAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "dateAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.day) || node.day?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'day' of ${locationDescription(node)} must have a value`, node, "day", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.year) || node.year?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'year' of ${locationDescription(node)} must have a value`, node, "year", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.month)) {
            this.errorList.push(
                new FreError(`Property 'month' of ${locationDescription(node)} must have a value`, node, "month", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventSchedule(node: EventSchedule): boolean {
        if (isNullOrUndefined(node.eventStart)) {
            this.errorList.push(
                new FreError(
                    `Property 'eventStart' of ${locationDescription(node)} must have a value`,
                    node,
                    "eventStart",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventReference(node: EventReference): boolean {
        if (isNullOrUndefined(node.event)) {
            this.errorList.push(
                new FreError(`Property 'event' of ${locationDescription(node)} must have a value`, node, "event", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.eventState)) {
            this.errorList.push(
                new FreError(
                    `Property 'eventState' of ${locationDescription(node)} must have a value`,
                    node,
                    "eventState",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTimeUnit(node: TimeUnit): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePhaseState(node: PhaseState): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventState(node: EventState): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSimpleOperators(node: SimpleOperators): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDays(node: Days): boolean {
        if (isNullOrUndefined(node.count)) {
            this.errorList.push(
                new FreError(`Property 'count' of ${locationDescription(node)} must have a value`, node, "count", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTimeAmount(node: TimeAmount): boolean {
        if (isNullOrUndefined(node.value)) {
            this.errorList.push(
                new FreError(`Property 'value' of ${locationDescription(node)} must have a value`, node, "value", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.unit)) {
            this.errorList.push(
                new FreError(`Property 'unit' of ${locationDescription(node)} must have a value`, node, "unit", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTimeAmountPart(node: TimeAmountPart): boolean {
        if (isNullOrUndefined(node.timeAmount)) {
            this.errorList.push(
                new FreError(
                    `Property 'timeAmount' of ${locationDescription(node)} must have a value`,
                    node,
                    "timeAmount",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.operator)) {
            this.errorList.push(
                new FreError(
                    `Property 'operator' of ${locationDescription(node)} must have a value`,
                    node,
                    "operator",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDay(node: Day): boolean {
        if (isNullOrUndefined(node.startDay)) {
            this.errorList.push(
                new FreError(
                    `Property 'startDay' of ${locationDescription(node)} must have a value`,
                    node,
                    "startDay",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeWhen(node: When): boolean {
        if (isNullOrUndefined(node.startWhen)) {
            this.errorList.push(
                new FreError(
                    `Property 'startWhen' of ${locationDescription(node)} must have a value`,
                    node,
                    "startWhen",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePrevious(node: Previous): boolean {
        if (isNullOrUndefined(node.eventState)) {
            this.errorList.push(
                new FreError(
                    `Property 'eventState' of ${locationDescription(node)} must have a value`,
                    node,
                    "eventState",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventWindow(node: EventWindow): boolean {
        if (isNullOrUndefined(node.daysBefore)) {
            this.errorList.push(
                new FreError(
                    `Property 'daysBefore' of ${locationDescription(node)} must have a value`,
                    node,
                    "daysBefore",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.daysAfter)) {
            this.errorList.push(
                new FreError(
                    `Property 'daysAfter' of ${locationDescription(node)} must have a value`,
                    node,
                    "daysAfter",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeComplianceWindowOf(node: ComplianceWindowOf): boolean {
        if (isNullOrUndefined(node.maxDaysBefore)) {
            this.errorList.push(
                new FreError(
                    `Property 'maxDaysBefore' of ${locationDescription(node)} must have a value`,
                    node,
                    "maxDaysBefore",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.maxDaysAfter)) {
            this.errorList.push(
                new FreError(
                    `Property 'maxDaysAfter' of ${locationDescription(node)} must have a value`,
                    node,
                    "maxDaysAfter",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStartingBy(node: StartingBy): boolean {
        if (isNullOrUndefined(node.startTimeAsString) || node.startTimeAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'startTimeAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "startTimeAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.startHour)) {
            this.errorList.push(
                new FreError(
                    `Property 'startHour' of ${locationDescription(node)} must have a value`,
                    node,
                    "startHour",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.startMinute)) {
            this.errorList.push(
                new FreError(
                    `Property 'startMinute' of ${locationDescription(node)} must have a value`,
                    node,
                    "startMinute",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEndingBy(node: EndingBy): boolean {
        if (isNullOrUndefined(node.endTimeAsString) || node.endTimeAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'endTimeAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "endTimeAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.endHour)) {
            this.errorList.push(
                new FreError(
                    `Property 'endHour' of ${locationDescription(node)} must have a value`,
                    node,
                    "endHour",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.endMinute)) {
            this.errorList.push(
                new FreError(
                    `Property 'endMinute' of ${locationDescription(node)} must have a value`,
                    node,
                    "endMinute",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeBetweenTimes(node: BetweenTimes): boolean {
        if (isNullOrUndefined(node.startTimeAsString) || node.startTimeAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'startTimeAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "startTimeAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.endTimeAsString) || node.endTimeAsString?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'endTimeAsString' of ${locationDescription(node)} must have a value`,
                    node,
                    "endTimeAsString",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.startHour)) {
            this.errorList.push(
                new FreError(
                    `Property 'startHour' of ${locationDescription(node)} must have a value`,
                    node,
                    "startHour",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.startMinute)) {
            this.errorList.push(
                new FreError(
                    `Property 'startMinute' of ${locationDescription(node)} must have a value`,
                    node,
                    "startMinute",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.endHour)) {
            this.errorList.push(
                new FreError(
                    `Property 'endHour' of ${locationDescription(node)} must have a value`,
                    node,
                    "endHour",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.endMinute)) {
            this.errorList.push(
                new FreError(
                    `Property 'endMinute' of ${locationDescription(node)} must have a value`,
                    node,
                    "endMinute",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRepeatCondition(node: RepeatCondition): boolean {
        if (isNullOrUndefined(node.maxRepeats)) {
            this.errorList.push(
                new FreError(
                    `Property 'maxRepeats' of ${locationDescription(node)} must have a value`,
                    node,
                    "maxRepeats",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.repeatUnit)) {
            this.errorList.push(
                new FreError(
                    `Property 'repeatUnit' of ${locationDescription(node)} must have a value`,
                    node,
                    "repeatUnit",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRepeatEvery(node: RepeatEvery): boolean {
        if (isNullOrUndefined(node.repeatEvery)) {
            this.errorList.push(
                new FreError(
                    `Property 'repeatEvery' of ${locationDescription(node)} must have a value`,
                    node,
                    "repeatEvery",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRepeatCount(node: RepeatCount): boolean {
        if (isNullOrUndefined(node.repeatCount)) {
            this.errorList.push(
                new FreError(
                    `Property 'repeatCount' of ${locationDescription(node)} must have a value`,
                    node,
                    "repeatCount",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePeriod(node: Period): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTypeOfEvent(node: TypeOfEvent): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEvent(node: Event): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.alternativeName) || node.alternativeName?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'alternativeName' of ${locationDescription(node)} must have a value`,
                    node,
                    "alternativeName",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showSequenced)) {
            this.errorList.push(
                new FreError(
                    `Property 'showSequenced' of ${locationDescription(node)} must have a value`,
                    node,
                    "showSequenced",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.eventNumber)) {
            this.errorList.push(
                new FreError(
                    `Property 'eventNumber' of ${locationDescription(node)} must have a value`,
                    node,
                    "eventNumber",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.schedule)) {
            this.errorList.push(
                new FreError(
                    `Property 'schedule' of ${locationDescription(node)} must have a value`,
                    node,
                    "schedule",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.typeOfEvent)) {
            this.errorList.push(
                new FreError(
                    `Property 'typeOfEvent' of ${locationDescription(node)} must have a value`,
                    node,
                    "typeOfEvent",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTask(node: Task): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.numberedSteps)) {
            this.errorList.push(
                new FreError(
                    `Property 'numberedSteps' of ${locationDescription(node)} must have a value`,
                    node,
                    "numberedSteps",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showDetails)) {
            this.errorList.push(
                new FreError(
                    `Property 'showDetails' of ${locationDescription(node)} must have a value`,
                    node,
                    "showDetails",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSharedTask(node: SharedTask): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.numberedSteps)) {
            this.errorList.push(
                new FreError(
                    `Property 'numberedSteps' of ${locationDescription(node)} must have a value`,
                    node,
                    "numberedSteps",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.showDetails)) {
            this.errorList.push(
                new FreError(
                    `Property 'showDetails' of ${locationDescription(node)} must have a value`,
                    node,
                    "showDetails",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTaskReference(node: TaskReference): boolean {
        if (isNullOrUndefined(node.task)) {
            this.errorList.push(
                new FreError(`Property 'task' of ${locationDescription(node)} must have a value`, node, "task", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStep(node: Step): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeWorkflowDescription(node: WorkflowDescription): boolean {
        if (isNullOrUndefined(node.text)) {
            this.errorList.push(
                new FreError(`Property 'text' of ${locationDescription(node)} must have a value`, node, "text", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeWorkflowBranch(node: WorkflowBranch): boolean {
        if (isNullOrUndefined(node.branch)) {
            this.errorList.push(
                new FreError(`Property 'branch' of ${locationDescription(node)} must have a value`, node, "branch", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSystemAccess(node: SystemAccess): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.isShared)) {
            this.errorList.push(
                new FreError(
                    `Property 'isShared' of ${locationDescription(node)} must have a value`,
                    node,
                    "isShared",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.accessedAt)) {
            this.errorList.push(
                new FreError(
                    `Property 'accessedAt' of ${locationDescription(node)} must have a value`,
                    node,
                    "accessedAt",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSystemAccessReference(node: SystemAccessReference): boolean {
        if (isNullOrUndefined(node.system)) {
            this.errorList.push(
                new FreError(`Property 'system' of ${locationDescription(node)} must have a value`, node, "system", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeStaffRole(node: StaffRole): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePersonReference(node: PersonReference): boolean {
        if (isNullOrUndefined(node.person)) {
            this.errorList.push(
                new FreError(`Property 'person' of ${locationDescription(node)} must have a value`, node, "person", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePerson(node: Person): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.role)) {
            this.errorList.push(
                new FreError(`Property 'role' of ${locationDescription(node)} must have a value`, node, "role", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeReference(node: Reference): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.description)) {
            this.errorList.push(
                new FreError(
                    `Property 'description' of ${locationDescription(node)} must have a value`,
                    node,
                    "description",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeRobotMapping(node: RobotMapping): boolean {
        if (isNullOrUndefined(node.name) || node.name?.length === 0) {
            this.errorList.push(
                new FreError(`Property 'name' of ${locationDescription(node)} must have a value`, node, "name", FreErrorSeverity.Error),
            );
        }
        if (isNullOrUndefined(node.sourcePage) || node.sourcePage?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'sourcePage' of ${locationDescription(node)} must have a value`,
                    node,
                    "sourcePage",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.targetPage) || node.targetPage?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'targetPage' of ${locationDescription(node)} must have a value`,
                    node,
                    "targetPage",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.closeSource)) {
            this.errorList.push(
                new FreError(
                    `Property 'closeSource' of ${locationDescription(node)} must have a value`,
                    node,
                    "closeSource",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is NOT considered 'fatal', which means that other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSourceToTargetMapping(node: SourceToTargetMapping): boolean {
        if (isNullOrUndefined(node.sourceField) || node.sourceField?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'sourceField' of ${locationDescription(node)} must have a value`,
                    node,
                    "sourceField",
                    FreErrorSeverity.Error,
                ),
            );
        }
        if (isNullOrUndefined(node.targetField) || node.targetField?.length === 0) {
            this.errorList.push(
                new FreError(
                    `Property 'targetField' of ${locationDescription(node)} must have a value`,
                    node,
                    "targetField",
                    FreErrorSeverity.Error,
                ),
            );
        }

        return false;
    }
}
