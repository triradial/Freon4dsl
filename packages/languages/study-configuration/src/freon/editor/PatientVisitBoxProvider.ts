// Generated by the Freon Language Generator.
// TEMPLATE BoxProviderTemplate.generateBoxProvider(...)

import {
    Box,
    BoxUtil,
    FreBoxProvider,
    FreProjectionHandler,
    FreNodeReference,
    type FreNamedNode,
    LimitedDisplay,
    BoxFactory,
    FragmentBox,
} from "@freon4dsl/core";
import { PatientVisit, Event, PatientVisitStatus } from "../language/index.js";

import { LanguageEnvironment } from "../index.js";

/**
 * This class implements the box provider for a single node of type PatientVisit.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class PatientVisitBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default", "parser"];
        this.knownTableProjections = ["default"];
        this.conceptName = "PatientVisit";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            } else if (projectionName === "parser") {
                return this.getParser();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxUtil.fragmentWrapperBox(this._node as PatientVisit, "SingleLineItemComponent", this.getFragmentBox_visitDetails(), {
            params: [
                { key: "cssClass", value: "type3" },
                { key: "canDelete", value: "true" },
                { key: "canAdd", value: "false" },
                { key: "canExpand", value: "true" },
                { key: "isExpanded", value: "true" },
                { key: "selectable", value: "true" },
                { key: "label", value: "Visit" },
            ],
        });
    }

    private getParser(): Box {
        return BoxFactory.horizontalLayout(
            this._node as PatientVisit,
            "PatientVisit-hlist-line-0",
            "",
            [
                BoxUtil.labelBox(this._node as PatientVisit, "Visit:", "top-1-line-0-item-0"),
                BoxUtil.referenceBox(
                    this._node as PatientVisit,
                    "visit",
                    (selected: string | FreNamedNode) => {
                        const ref =
                            typeof selected === "string"
                                ? FreNodeReference.create<Event>(selected, "Event")
                                : FreNodeReference.create<Event>(selected as Event, "Event");
                        (this._node as PatientVisit).visit = ref;
                    },
                    LanguageEnvironment.getInstance().scoper,
                ),
                BoxUtil.labelBox(this._node as PatientVisit, "was", "top-1-line-0-item-2"),
                BoxUtil.limitedBox(
                    this._node as PatientVisit,
                    "status",
                    (selected: string | PatientVisitStatus) => {
                        (this._node as PatientVisit).status = FreNodeReference.create<PatientVisitStatus>(selected, "PatientVisitStatus");
                    },
                    LimitedDisplay.SELECT,
                    LanguageEnvironment.getInstance().scoper,
                ),
                BoxUtil.labelBox(this._node as PatientVisit, "on", "top-1-line-0-item-4"),
                BoxUtil.getBoxOrAction(this._node as PatientVisit, "actualVisitDate", "DateConcept", this.mainHandler),
            ],
            { selectable: false },
        );
    }

    private getFragmentBox_visitDetails(): FragmentBox {
        return new FragmentBox(
            this._node as PatientVisit,
            "PatientVisit-fragment-visitDetails",
            BoxFactory.horizontalLayout(
                this._node as PatientVisit,
                "PatientVisit-fragment-visitDetails-hlist-line-0",
                "",
                [
                    BoxUtil.referenceBox(
                        this._node as PatientVisit,
                        "visit",
                        (selected: string | FreNamedNode) => {
                            const ref =
                                typeof selected === "string"
                                    ? FreNodeReference.create<Event>(selected, "Event")
                                    : FreNodeReference.create<Event>(selected as Event, "Event");
                            (this._node as PatientVisit).visit = ref;
                        },
                        LanguageEnvironment.getInstance().scoper,
                    ),
                    BoxUtil.labelBox(this._node as PatientVisit, "was", "top-1-line-0-item-1"),
                    BoxUtil.limitedBox(
                        this._node as PatientVisit,
                        "status",
                        (selected: string | PatientVisitStatus) => {
                            (this._node as PatientVisit).status = FreNodeReference.create<PatientVisitStatus>(
                                selected,
                                "PatientVisitStatus",
                            );
                        },
                        LimitedDisplay.SELECT,
                        LanguageEnvironment.getInstance().scoper,
                    ),
                    BoxUtil.labelBox(this._node as PatientVisit, "on", "top-1-line-0-item-3"),
                    BoxUtil.getBoxOrAction(this._node as PatientVisit, "actualVisitDate", "DateConcept", this.mainHandler),
                ],
                { selectable: false },
            ),
            { cssClass: "PatientVisit-fragment-visitDetails" },
        );
    }
}
