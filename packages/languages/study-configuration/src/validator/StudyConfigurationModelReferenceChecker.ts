// Generated by the Freon Language Generator.
// TEMPLATE ReferenceCheckerTemplate.generateChecker(...)

import {
    FreError,
    FreErrorSeverity,
    type FreWriter,
    FreNodeReference,
    type FreNamedNode,
    type FreNode,
    FreLanguageEnvironment,
} from "@freon4dsl/core";
import {
    EventReferenceExpression,
    PeriodReferenceExpression,
    Time,
    CountExpression,
    PatientVisit,
    VisitDate,
    DateConcept,
    EventReference,
    TimeAmount,
    TimeAmountPart,
    Previous,
    Event,
    TaskReference,
    SystemAccessReference,
    PersonReference,
    Person,
} from "../language/index.js";

import { StudyConfigurationModelDefaultWorker } from "../utils/index.js";
import { type StudyConfigurationModelCheckerInterface } from "./StudyConfigurationModelValidator.js";
import { locationDescription } from "../../custom/CustomLocationDescription.js";

/**
 * Class StudyConfigurationModelReferenceChecker is part of the implementation of the default validator.
 * It checks whether references can be found within the model.
 *
 * Class StudyConfigurationModelWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class StudyConfigurationModelReferenceChecker
    extends StudyConfigurationModelDefaultWorker
    implements StudyConfigurationModelCheckerInterface
{
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];
    private refSeparator: string = "/";

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventReferenceExpression(node: EventReferenceExpression): boolean {
        if (!!node.event && node.event.referred === null) {
            this.makeErrorMessage(node, node.event, "event");
        }
        if (!!node.eventState && node.eventState.referred === null) {
            this.makeErrorMessage(node, node.eventState, "eventState");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePeriodReferenceExpression(node: PeriodReferenceExpression): boolean {
        if (!!node.period && node.period.referred === null) {
            this.makeErrorMessage(node, node.period, "period");
        }
        if (!!node.periodState && node.periodState.referred === null) {
            this.makeErrorMessage(node, node.periodState, "periodState");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTime(node: Time): boolean {
        if (!!node.unit && node.unit.referred === null) {
            this.makeErrorMessage(node, node.unit, "unit");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeCountExpression(node: CountExpression): boolean {
        if (!!node.event && node.event.referred === null) {
            this.makeErrorMessage(node, node.event, "event");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePatientVisit(node: PatientVisit): boolean {
        if (!!node.visit && node.visit.referred === null) {
            this.makeErrorMessage(node, node.visit, "visit");
        }
        if (!!node.status && node.status.referred === null) {
            this.makeErrorMessage(node, node.status, "status");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeVisitDate(node: VisitDate): boolean {
        if (!!node.month && node.month.referred === null) {
            this.makeErrorMessage(node, node.month, "month");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDateConcept(node: DateConcept): boolean {
        if (!!node.month && node.month.referred === null) {
            this.makeErrorMessage(node, node.month, "month");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEventReference(node: EventReference): boolean {
        if (!!node.event && node.event.referred === null) {
            this.makeErrorMessage(node, node.event, "event");
        }
        if (!!node.eventState && node.eventState.referred === null) {
            this.makeErrorMessage(node, node.eventState, "eventState");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTimeAmount(node: TimeAmount): boolean {
        if (!!node.unit && node.unit.referred === null) {
            this.makeErrorMessage(node, node.unit, "unit");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTimeAmountPart(node: TimeAmountPart): boolean {
        if (!!node.operator && node.operator.referred === null) {
            this.makeErrorMessage(node, node.operator, "operator");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePrevious(node: Previous): boolean {
        if (!!node.eventState && node.eventState.referred === null) {
            this.makeErrorMessage(node, node.eventState, "eventState");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeEvent(node: Event): boolean {
        if (!!node.typeOfEvent && node.typeOfEvent.referred === null) {
            this.makeErrorMessage(node, node.typeOfEvent, "typeOfEvent");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeTaskReference(node: TaskReference): boolean {
        if (!!node.task && node.task.referred === null) {
            this.makeErrorMessage(node, node.task, "task");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeSystemAccessReference(node: SystemAccessReference): boolean {
        if (!!node.system && node.system.referred === null) {
            this.makeErrorMessage(node, node.system, "system");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePersonReference(node: PersonReference): boolean {
        if (!!node.person && node.person.referred === null) {
            this.makeErrorMessage(node, node.person, "person");
        }
        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorList'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePerson(node: Person): boolean {
        if (!!node.role && node.role.referred === null) {
            this.makeErrorMessage(node, node.role, "role");
        }
        return false;
    }

    private makeErrorMessage(node: FreNode, referredElem: FreNodeReference<FreNamedNode>, propertyName: string) {
        const scoper = FreLanguageEnvironment.getInstance().scoper;
        const possibles = scoper.getVisibleNodes(node).filter((elem) => elem.name === referredElem.name);
        if (possibles.length > 0) {
            this.errorList.push(
                new FreError(
                    `Reference '${referredElem.pathnameToString(this.refSeparator)}' should have type '${referredElem.typeName}', but found type(s) [${possibles.map((elem) => `${elem.freLanguageConcept()}`).join(", ")}] in ${propertyName} of ${locationDescription(node)}`,
                    node,
                    `${propertyName}`,
                    FreErrorSeverity.Error,
                ),
            );
        } else {
            this.errorList.push(
                new FreError(
                    `Cannot find reference '${referredElem.pathnameToString(this.refSeparator)}' in ${propertyName} of ${locationDescription(node)}`,
                    node,
                    `${propertyName}`,
                    FreErrorSeverity.Error,
                ),
            );
        }
    }
}
