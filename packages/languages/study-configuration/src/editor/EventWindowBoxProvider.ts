// Generated by the Freon Language Generator.
// TEMPLATE BoxProviderTemplate.generateBoxProvider(...)

import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";
import { EventWindow } from "../language/index.js";

/**
 * This class implements the box provider for a single node of type EventWindow.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class EventWindowBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default", "parser"];
        this.knownTableProjections = ["default"];
        this.conceptName = "EventWindow";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            } else if (projectionName === "parser") {
                return this.getParser();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(
            this._node as EventWindow,
            "EventWindow-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as EventWindow,
                    "EventWindow-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as EventWindow, "with a window of", "top-1-line-0-item-0"),
                        BoxUtil.partReplacerBox(this._node as EventWindow, "daysBefore", "CustomNumericComponent", {
                            params: [
                                { key: "min", value: "0" },
                                { key: "max", value: "30" },
                            ],
                        }),
                        ,
                        BoxUtil.labelBox(this._node as EventWindow, "day(s) before and", "top-1-line-0-item-2"),
                        BoxUtil.partReplacerBox(this._node as EventWindow, "daysAfter", "CustomNumericComponent", {
                            params: [
                                { key: "min", value: "0" },
                                { key: "max", value: "30" },
                            ],
                        }),
                        ,
                        BoxUtil.labelBox(this._node as EventWindow, "day(s) after", "top-1-line-0-item-4"),
                    ],
                    { selectable: false },
                ),
                BoxUtil.partReplacerBox(this._node as EventWindow, "complianceWindow", "CustomActionsComponent", {
                    params: [{ key: "placeholder", value: "with no compliance window" }],
                }),
            ],
            { cssClass: "EventWindow" },
        );
    }

    private getParser(): Box {
        return BoxFactory.verticalLayout(
            this._node as EventWindow,
            "EventWindow-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as EventWindow,
                    "EventWindow-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as EventWindow, "with a window of", "top-1-line-0-item-0"),
                        BoxUtil.getBoxOrAction(this._node as EventWindow, "daysBefore", "Days", this.mainHandler),
                        BoxUtil.labelBox(this._node as EventWindow, "day(s) before and", "top-1-line-0-item-2"),
                        BoxUtil.getBoxOrAction(this._node as EventWindow, "daysAfter", "Days", this.mainHandler),
                        BoxUtil.labelBox(this._node as EventWindow, "day(s) after", "top-1-line-0-item-4"),
                    ],
                    { selectable: false },
                ),
                BoxUtil.getBoxOrAction(this._node as EventWindow, "complianceWindow", "ComplianceWindow", this.mainHandler),
            ],
            { cssClass: "EventWindow" },
        );
    }
}
