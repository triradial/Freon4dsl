// Generated by the Freon Language Generator.
// TEMPLATE BoxProviderTemplate.generateBoxProvider(...)

import { Box, BoxUtil, FreBoxProvider, FreProjectionHandler, BoxFactory } from "@freon4dsl/core";
import { EventSchedule } from "../language/index.js";

/**
 * This class implements the box provider for a single node of type EventSchedule.
 * The box provider is able to create the (tree of) boxes for the node, based
 * on the projections that are currently selected by the user.
 * The top of the tree of boxes is always a box of type ElementBox, which is
 * a box that will never be rendered itself, only its content will. Thus, we
 * have a stable entry in the complete box tree for every FreNode node.
 */
export class EventScheduleBoxProvider extends FreBoxProvider {
    constructor(mainHandler: FreProjectionHandler) {
        super(mainHandler);
        this.knownBoxProjections = ["default", "parser"];
        this.knownTableProjections = ["default"];
        this.conceptName = "EventSchedule";
    }

    protected getContent(projectionName: string): Box {
        // console.log("GET CONTENT " + this._node?.freId() + ' ' +  this._node?.freLanguageConcept() + ' ' + projectionName);
        // see if we need to use a custom projection
        if (!this.knownBoxProjections.includes(projectionName) && !this.knownTableProjections.includes(projectionName)) {
            const BOX: Box = this.mainHandler.executeCustomProjection(this._node, projectionName);
            if (!!BOX) {
                // found one, so return it
                return BOX;
            }
        } else {
            // select the box to return based on the projectionName
            if (projectionName === "default") {
                return this.getDefault();
            } else if (projectionName === "parser") {
                return this.getParser();
            }
        }
        // in all other cases, return the default
        return this.getDefault();
    }

    private getDefault(): Box {
        return BoxFactory.verticalLayout(
            this._node as EventSchedule,
            "EventSchedule-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as EventSchedule,
                    "EventSchedule-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as EventSchedule, "First scheduled", "top-1-line-0-item-0"),
                        BoxUtil.partReplacerBox(this._node as EventSchedule, "eventStart", "CustomActionsComponent", {
                            params: [{ key: "placeholder", value: "event start" }],
                        }),
                    ],
                    { selectable: false },
                ),
                BoxUtil.partReplacerBox(this._node as EventSchedule, "eventWindow", "CustomActionsComponent", {
                    params: [{ key: "placeholder", value: "with no window" }],
                }),
                ,
                BoxUtil.partReplacerBox(this._node as EventSchedule, "eventRepeat", "CustomActionsComponent", {
                    params: [{ key: "placeholder", value: "with no repetitions" }],
                }),
                ,
                BoxUtil.partReplacerBox(this._node as EventSchedule, "eventTimeOfDay", "CustomActionsComponent", {
                    params: [{ key: "placeholder", value: "with no limit on the time of day" }],
                }),
            ],
            { cssClass: "EventSchedule" },
        );
    }

    private getParser(): Box {
        return BoxFactory.verticalLayout(
            this._node as EventSchedule,
            "EventSchedule-overall",
            "",
            [
                BoxFactory.horizontalLayout(
                    this._node as EventSchedule,
                    "EventSchedule-hlist-line-0",
                    "",
                    [
                        BoxUtil.labelBox(this._node as EventSchedule, "First scheduled", "top-1-line-0-item-0"),
                        BoxUtil.getBoxOrAction(this._node as EventSchedule, "eventStart", "EventStart", this.mainHandler),
                    ],
                    { selectable: false },
                ),
                BoxUtil.getBoxOrAction(this._node as EventSchedule, "eventWindow", "EventWindow", this.mainHandler),
                BoxFactory.optional(
                    this._node as EventSchedule,
                    "optional-eventRepeat",
                    () => !!(this._node as EventSchedule).eventRepeat,
                    BoxFactory.horizontalLayout(
                        this._node as EventSchedule,
                        "EventSchedule-optional-eventRepeat-hlist-line-0",
                        "",
                        [
                            BoxUtil.labelBox(this._node as EventSchedule, "and then repeats", "top-2-line-0-item-0"),
                            BoxUtil.getBoxOrAction(this._node as EventSchedule, "eventRepeat", "RepeatExpression", this.mainHandler),
                        ],
                        { selectable: false },
                    ),
                    false,
                    BoxFactory.action(this._node, "optional-eventRepeat", "and then repeats"),
                ),
                BoxFactory.optional(
                    this._node as EventSchedule,
                    "optional-eventTimeOfDay",
                    () => !!(this._node as EventSchedule).eventTimeOfDay,
                    BoxFactory.horizontalLayout(
                        this._node as EventSchedule,
                        "EventSchedule-optional-eventTimeOfDay-hlist-line-0",
                        "",
                        [
                            BoxUtil.labelBox(this._node as EventSchedule, "limited to", "top-2-line-0-item-0"),
                            BoxUtil.getBoxOrAction(this._node as EventSchedule, "eventTimeOfDay", "EventTimeOfDay", this.mainHandler),
                        ],
                        { selectable: false },
                    ),
                    false,
                    BoxFactory.action(this._node, "optional-eventTimeOfDay", "limited to"),
                ),
            ],
            { cssClass: "EventSchedule" },
        );
    }
}
