// Generated by the Freon Language Generator.
import { type KtList, type Sentence, type SpptDataNodeInfo } from "net.akehurst.language-agl-processor";
import { FreNodeReference, notNullOrUndefined } from "@freon4dsl/core";
import { DateConcept, DateRange, PatientHistory, PatientVisit, Event, PatientVisitStatus, Month } from "../language/index.js";
import { PrimValueType, StudyConfigurationModelSyntaxAnalyser, ParsedNodeReference } from "./StudyConfigurationModelSyntaxAnalyser.js";

export class StudyConfigurationModelCommonSyntaxAnalyserPart {
    mainAnalyser: StudyConfigurationModelSyntaxAnalyser;

    constructor(mainAnalyser: StudyConfigurationModelSyntaxAnalyser) {
        this.mainAnalyser = mainAnalyser;
    }

    /**
     * Method to transform branches that match the following rule:
     * DateConcept = stringLiteral ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformDateConcept(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): DateConcept {
        // console.log('4 transformDateConcept called: ' + children.toString());
        let __dateAsString: string;
        __dateAsString = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[0] as unknown as string,
            PrimValueType.string,
        ); // RHSPrimEntry

        return DateConcept.create({
            dateAsString: __dateAsString!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * DateRange = DateConcept 'thru:' DateConcept? ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformDateRange(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): DateRange {
        // console.log('4 transformDateRange called: ' + children.toString());
        let __startDate: DateConcept;
        let __endDate: DateConcept;
        __startDate = children.asJsReadonlyArrayView()[0] as DateConcept; // RHSPartEntry
        // RHSPartOptionalEntry
        if (!!children.asJsReadonlyArrayView()[2]) {
            __endDate = children.asJsReadonlyArrayView()[2];
        }
        return DateRange.create({
            startDate: __startDate!,
            endDate: __endDate!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * PatientHistory = 'Patient' 'ID:' stringLiteral
     *	 'Visits:'
     *	 PatientVisit*
     *	 'Not' 'Available' 'Dates:'
     *	 DateRange* ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformPatientHistory(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): PatientHistory {
        // console.log('4 transformPatientHistory called: ' + children.toString());
        let __patient_id: string;
        let __patientVisits: PatientVisit[];
        let __patientNotAvailableDates: DateRange[];
        __patient_id = this.mainAnalyser.transformPrimValue<string>(
            children.asJsReadonlyArrayView()[2] as unknown as string,
            PrimValueType.string,
        ); // RHSPrimEntry
        __patientVisits = (children.asJsReadonlyArrayView()[4] as KtList<any>).asJsReadonlyArrayView() as PatientVisit[]; // RHSPartListEntry
        __patientNotAvailableDates = (children.asJsReadonlyArrayView()[8] as KtList<any>).asJsReadonlyArrayView() as DateRange[]; // RHSPartListEntry

        return PatientHistory.create({
            patient_id: __patient_id!,
            patientVisits: __patientVisits!,
            patientNotAvailableDates: __patientNotAvailableDates!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * PatientVisit = 'Visit:' __fre_reference 'was' PatientVisitStatus 'on' DateConcept ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformPatientVisit(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): PatientVisit {
        // console.log('4 transformPatientVisit called: ' + children.toString());
        let __visit: FreNodeReference<Event>;
        let __status: FreNodeReference<PatientVisitStatus>;
        let __actualVisitDate: DateConcept;
        __visit = this.mainAnalyser.makeFreNodeRef<Event>(children.asJsReadonlyArrayView()[1] as ParsedNodeReference, "Event"); // RHSRefEntry
        __status = children.asJsReadonlyArrayView()[3] as FreNodeReference<PatientVisitStatus>; // RHSLimitedRefEntry
        __actualVisitDate = children.asJsReadonlyArrayView()[5] as DateConcept; // RHSPartEntry

        return PatientVisit.create({
            visit: __visit!,
            status: __status!,
            actualVisitDate: __actualVisitDate!,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * Month = '\`January\`'
     *	| '\`February\`'
     *	| '\`March\`'
     *	| '\`April\`'
     *	| '\`May\`'
     *	| '\`June\`'
     *	| '\`July\`'
     *	| '\`August\`'
     *	| '\`September\`'
     *	| '\`October\`'
     *	| '\`November\`'
     *	| '\`December\`' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformMonth(nodeInfo: SpptDataNodeInfo, children: KtList<any>, sentence: Sentence): FreNodeReference<Month> | undefined {
        // console.log('5 transformMonth called: ' + children.toString());
        let result: FreNodeReference<Month> | undefined;
        const child: string = children.asJsReadonlyArrayView()[0];
        // todo make sure we remove only the outer quotes
        const name: string = child.replace(/`/g, "");
        switch (name) {
            case "January": {
                result = FreNodeReference.create<Month>(Month.January, "Month");
                break;
            }
            case "February": {
                result = FreNodeReference.create<Month>(Month.February, "Month");
                break;
            }
            case "March": {
                result = FreNodeReference.create<Month>(Month.March, "Month");
                break;
            }
            case "April": {
                result = FreNodeReference.create<Month>(Month.April, "Month");
                break;
            }
            case "May": {
                result = FreNodeReference.create<Month>(Month.May, "Month");
                break;
            }
            case "June": {
                result = FreNodeReference.create<Month>(Month.June, "Month");
                break;
            }
            case "July": {
                result = FreNodeReference.create<Month>(Month.July, "Month");
                break;
            }
            case "August": {
                result = FreNodeReference.create<Month>(Month.August, "Month");
                break;
            }
            case "September": {
                result = FreNodeReference.create<Month>(Month.September, "Month");
                break;
            }
            case "October": {
                result = FreNodeReference.create<Month>(Month.October, "Month");
                break;
            }
            case "November": {
                result = FreNodeReference.create<Month>(Month.November, "Month");
                break;
            }
            case "December": {
                result = FreNodeReference.create<Month>(Month.December, "Month");
                break;
            }
            default:
                result = undefined;
        }
        if (result !== undefined) {
            result.parseLocation = this.mainAnalyser.location(sentence, nodeInfo.node);
        }
        return result;
    }

    /**
     * Method to transform branches that match the following rule:
     * PatientVisitStatus = '\`planned\`'
     *	| '\`completed\`'
     *	| '\`missed\`'
     *	| '\`canceled\`' ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformPatientVisitStatus(
        nodeInfo: SpptDataNodeInfo,
        children: KtList<any>,
        sentence: Sentence,
    ): FreNodeReference<PatientVisitStatus> | undefined {
        // console.log('5 transformPatientVisitStatus called: ' + children.toString());
        let result: FreNodeReference<PatientVisitStatus> | undefined;
        const child: string = children.asJsReadonlyArrayView()[0];
        // todo make sure we remove only the outer quotes
        const name: string = child.replace(/`/g, "");
        switch (name) {
            case "planned": {
                result = FreNodeReference.create<PatientVisitStatus>(PatientVisitStatus.planned, "PatientVisitStatus");
                break;
            }
            case "completed": {
                result = FreNodeReference.create<PatientVisitStatus>(PatientVisitStatus.completed, "PatientVisitStatus");
                break;
            }
            case "missed": {
                result = FreNodeReference.create<PatientVisitStatus>(PatientVisitStatus.missed, "PatientVisitStatus");
                break;
            }
            case "canceled": {
                result = FreNodeReference.create<PatientVisitStatus>(PatientVisitStatus.canceled, "PatientVisitStatus");
                break;
            }
            default:
                result = undefined;
        }
        if (result !== undefined) {
            result.parseLocation = this.mainAnalyser.location(sentence, nodeInfo.node);
        }
        return result;
    }
}
