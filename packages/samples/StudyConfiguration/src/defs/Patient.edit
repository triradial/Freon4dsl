editor default

global {
    external {
        DatePicker,
        TimePicker,
        ExpandCollapseWrapper
    }
}

Availability {
  [
    STAFFING
      Number of CRCs Normally Available: ${baselineStaff}

      Staffing Level Changes:
      ${staffLevels}

  ]
}

PatientNotAvailable {
  [
  ${dates}
  ]
}

StaffLevel {
  [ 
    # Staff: ${staffAvailable} on: ${dateOrRange}
  ] trigger = "Staff Level"
}

DateRange {
  [${startDate} thru:${endDate}]
}

PatientInfo {
  [
   ${patientHistories}
  ]
}

PatientHistory {
  [
   ID ${patient_id} 
   Patient completed: 
   ${patientVisits} 
   Not Available ${patientNotAvailableDates}
  ]
}

PatientVisit {
  [
      Visit: '${visit}' on  ${actualVisitDateAsString replace=DatePicker startDate="30/09/2024"}
  ]
}

PatientHistoryUnit {
  [
    ${patientHistory}
  ]
}